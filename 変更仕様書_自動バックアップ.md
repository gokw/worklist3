# worklist3 変更仕様書：データ自動バックアップ機能（縮小スコープ版）

> 本書は Claude Code に実装を依頼するための変更仕様書です。検討の結果、対象を「ブラウザのデータ削除・破損による全消失を防ぐ」ことに絞り込みました。IndexedDB化・複数PC同期・大規模スナップショットは今回のスコープ外です（末尾の付録に検討経緯を残す）。

## 1. 目的（絞り込み後）

worklist3 のタスクデータは `localStorage`（キー `worklist3.tasks.v1`）にのみ保存されており、ブラウザの「サイトデータ削除」やキャッシュクリア、プロファイル破損で**全データが一撃で消失する**リスクがある。本変更の目的は、**変更のたびに自動でオフサイト（クラウド同期フォルダ）へ控えを取り、消失しても復元できる状態を作ること**。個人利用アプリとして過剰にならない最小構成で実装する。

## 2. 潰す対象と、あえて対象外にするもの

| シナリオ | 今回の扱い | 対応 |
|---|---|---|
| ブラウザのデータ削除・キャッシュクリアで全消失 | **対象** | 同期フォルダへの自動ミラー |
| localStorage 破損 → 空データで控えまで潰れる（破損の伝播） | **対象** | サニティガード＋数世代ローテーション |
| PC故障・紛失 | 副次的に対象 | ミラー先をクラウド同期フォルダにすることで自動的にオフサイト化 |
| 一括編集・インポート等の誤操作を過去へ戻す | 対象外 | 数世代ローテーションで部分的に救えるが、主目的ではない |
| 複数PCでの共有・同期 | 対象外（別テーマ） | 今回は扱わない |
| 容量上限（数年後の localStorage 枯渇） | 対象外（将来） | 将来アーカイブ機能で対応（付録参照） |

## 3. 決定事項（設計方針）

1. **`localStorage` は主保存のまま維持する。** アプリの動作・オフライン性は現状どおり。バックアップ層を付加機能として上に足す（既存の `TaskRepository` 差し替え可能な設計は活かす）。
2. **バックアップ先はユーザーが指定したローカルの同期フォルダ**とする（OneDrive / Box 等のクラウド同期フォルダを想定）。クラウド側のバージョン履歴が世代管理を上乗せしてくれることを狙う。
3. **書き込みは File System Access API** を用い、初回にフォルダ許可を1回取得すれば、以降は変更のたびに自動で書き出す。
4. **多重化は「ミラー＋最小ローテーション」の構成**とする。時間ごと112世代のような大掛かりな仕組みは採らない。
   - **ミラー**：変更のたび（デバウンス）に最新状態を単一ファイル `worklist3.json` へ上書き。
   - **ローテーション**：破損伝播の保険として、直近数世代の日次コピーのみ保持する。
5. **破損の伝播を止めるサニティガードを必須とする**（§4.3）。これが無いと「破損 → 空データを控えに焼き付け → 全消失」を防げない。
6. **復元は既存のインポート機能を流用する**（新規の復元UIは作らない）。

## 4. 機能要件（詳細仕様）

### 4.1 バックアップ先フォルダの指定

- 「バックアップ先フォルダを選択」操作を提供する（UI詳細は §6）。押下時に `window.showDirectoryPicker()` を呼び、ディレクトリハンドルを取得する。
- 取得したディレクトリハンドルは **IndexedDB に永続化**し、次回起動時に復元する（※これはデータ本体の保存先変更ではなく、ハンドル1個の保存のみ。データ本体は引き続き localStorage）。復元時は `queryPermission({ mode: "readwrite" })` で権限を確認し、必要なら `requestPermission` で再許可を求める。ブラウザ仕様上、再許可はユーザー操作起点が必要な場合があるため、その際は「バックアップ先を再接続」を促す。
- フォルダ未指定・権限なしの状態でも、アプリ本体は現状どおり動作すること（バックアップは付加機能。無効時も業務は止まらない）。

### 4.2 ミラー書き出し（ほぼ常時）

- タスクデータが変化するたびに、指定フォルダ直下へ最新の全タスクを **`worklist3.json`** として上書き保存する。
- 連続変更で書き込みが多発しないよう、**デバウンス（目安 1〜2秒）** を挟み、満了時点の最新状態を1回書き込む。
- 書き込み内容は現行の `exportTasksAsJson` と同等（`JSON.stringify(tasks, null, 2)`。整形済み・人が読める形式）。
- **原子的書き込み**：一時ファイル（例 `worklist3.json.tmp`）へ書いてから正式名へリネームし、同期途中の半端なファイルを残さない。

### 4.3 サニティガード（破損伝播の防止）※必須

ミラー／ローテーション書き込みの直前に、**書き込もうとしているデータが「壊れた結果の空／激減」でないか**を検査し、疑わしい場合は上書きを中止する。

- 書き込み対象が空配列（0件）で、かつ直前のバックアップが1件以上あった場合は、**自動上書きしない**。
- 件数が直前バックアップから急激に減少した場合（例：前回比で一定割合以上・かつ一定件数以上の減少）は、自動上書きを保留し、ユーザーへ通知する（トースト）。ユーザーが明示操作したとき（明示エクスポート等）は通常どおり書き込んでよい。
- 目的は「破損・パース失敗で `load()` が `[]` を返した状態を、無傷の控えへ焼き付けてしまう事故」を防ぐこと。しきい値は実装時に妥当な既定値を置き、必要なら微調整する（§9）。

> 参考：現状 `LocalStorageRepository.load()` は破損時に `[]` を返す。この空をそのままミラーへ書くと控えまで空になるため、本ガードで止める。

### 4.4 ローテーション（破損伝播の保険）

- ミラーとは別に、指定フォルダ配下の **`backups/`** サブフォルダへ日次コピーを保持する。
- ファイル名は **`worklist3-YYYY-MM-DD.json`**（例：`worklist3-2026-07-15.json`）。同一日の書き込みは同名ファイルへ上書き（＝1日1ファイル、その日の最終状態）。
- **保持世代は少数（既定 7 日程度）**。それより古いものは自動削除する。削除はファイル名の日付で判定し、`worklist3-` プレフィックスと `.json` 拡張子に一致するもののみを対象にする（無関係なファイルは触らない）。
- 削除処理はアプリ起動時に1回、加えて書き込み後に実行する。
- サニティガード（§4.3）はローテーション書き込みにも適用する。

### 4.5 復元

- 既存のインポート機能（`handleImportFile` / データメニューの「インポート」）を復元手段として利用する。`worklist3.json` あるいは `backups/` 内の任意ファイルを選んで読み込めば復元できる。
- 現状のインポートは「同一IDはスキップ（マージ）」挙動。全消失からの復元では既存が空なので、この挙動のままで全件復元できる。**新規の復元専用UIは作らない**（スコープ最小化）。

### 4.6 状態表示

- 現在のバックアップ状態をユーザーが把握できるようにする。最低限、次を表示する。
  - バックアップ先が接続済みか未接続か（接続済みならフォルダ名）。
  - 最終バックアップ成功時刻。
  - 直近の書き込みでエラー／ガード保留が起きた場合はその旨（例：権限切れ→再接続を促す、急減のため保留した等）。

## 5. 実装方針（技術メモ）

- **新規モジュール `src/lib/backup.ts`（想定）** を追加し、以下を担わせる。
  - ディレクトリハンドルの取得（`showDirectoryPicker`）と IndexedDB への保存/復元、権限確認（`queryPermission` / `requestPermission`）。
  - ミラー書き出し（デバウンス付き・原子的書き込み）：`getFileHandle("worklist3.json.tmp", { create: true })` → `createWritable()` → `write()` → `close()` → 正式名へ差し替え。
  - サニティガード（§4.3）：直前バックアップの件数を保持し、書き込み前に比較・判定。
  - ローテーション：`getDirectoryHandle("backups", { create: true })` → 日付別ファイル名で書き込み、古い世代を `removeEntry()` で削除。
  - 状態（接続状態・最終成功時刻・最終エラー/保留）の通知（コールバック or 小さなストアで App に伝える）。
  - 書き込みは直列化し、同時実行が起きないようにする（前の `createWritable` が閉じてから次を実行）。
- **`src/App.tsx`**：タスク保存の副作用（`useEffect([tasks])`）に、バックアップ層への通知を追加する。`localStorage` 保存は同期・即時のまま維持し、フォルダ書き込みは非同期・デバウンスで行い、保存の主経路をブロックしない。バックアップ状態を state で保持し Toolbar へ渡す。起動時にハンドル復元とローテーションの初期クリーンアップを呼ぶ。
- **`src/lib/storage.ts`**：バックアップ用のシリアライズ処理（`exportTasksAsJson` のJSON生成部）を再利用可能な関数として切り出し、`backup.ts` から使えるようにする。**データ本体の保存先（localStorage）は変更しない。**
- **`src/components/Toolbar.tsx`**：既存のデータメニュー（💾▾：エクスポート/インポート）に、バックアップ先の選択/再接続/解除と状態表示を追加する（§6）。
- **対応ブラウザ**：File System Access API は Chromium系（Chrome / Edge）で動作する。要件.md の「PCブラウザ／個人利用」前提に照らし Chrome / Edge を対象とする。API 非対応環境では機能を隠す／グレーアウトし、従来の手動エクスポート/インポートにフォールバックする。

## 6. UI変更点

既存のデータメニュー（Toolbar の `💾 ▾` 内、現状は「エクスポート」「インポート」の2項目）を拡張する。

- 未接続時：「**バックアップ先フォルダを選択**」項目を追加。押下で `showDirectoryPicker` を起動。
- 接続済み時：「**バックアップ先：〈フォルダ名〉（接続中）**」を表示し、「**再接続**」「**接続を解除**」を提供。
- 「**最終バックアップ：HH:MM 成功 / 保留 / エラー**」の状態表示。
- 既存の「エクスポート」「インポート」はそのまま残す（手動手段・復元手段として有効）。
- 権限切れ・書き込み失敗・ガード保留は、既存のトースト通知（`showToast`）で知らせ、必要なら再接続を促す。

## 7. ファイル別・変更概要

| 対象 | 種別 | 変更概要 |
|---|---|---|
| `src/lib/backup.ts` | 新規 | File System Access API によるミラー／日次ローテーション書き出し、サニティガード、ハンドルのIndexedDB永続化、権限確認、状態通知、原子的書き込み・直列化 |
| `src/lib/storage.ts` | 変更 | バックアップ用シリアライズを再利用可能な関数に整理（`exportTasksAsJson` のJSON生成部を切り出し）。**保存先は変更しない** |
| `src/App.tsx` | 変更 | タスク保存の副作用にバックアップ通知を追加、状態stateの保持、起動時のハンドル復元・世代クリーンアップ呼び出し、Toolbar への状態受け渡し |
| `src/components/Toolbar.tsx` | 変更 | データメニューにバックアップ先の選択/再接続/解除・状態表示を追加 |

## 8. 非機能・エッジケース

- **業務を止めない**：フォルダ未接続・権限拒否・書き込み失敗のいずれでも、`localStorage` による通常動作は維持されること。
- **破損の焼き付け防止**：サニティガード（§4.3）を必ず通す。空・急減データを自動で控えへ書かない。
- **原子的書き込み**：一時ファイル→リネームで、同期途中の半端ファイルを読ませない。
- **書き込みの直列化**：デバウンス満了時に1回、前の書き込み完了後に次を実行。多重実行を避ける。
- **時刻・タイムゾーン**：ローテーションのファイル名はローカル日付 `YYYY-MM-DD`（既存コードのローカル日付文字列と整合）。
- **パフォーマンス**：個人利用・小規模想定のため全件シリアライズ＋書き込みで十分（要件.md §8 準拠）。

## 9. 要検討事項（実装前に判断したい点）

- サニティガードの「急減」しきい値の既定値（例：前回比 50% 以上減 かつ 5件以上減で保留、等）。
- ローテーションの保持世代数（既定案：7日）。
- バックアップ状態の表示場所（データメニュー内で十分か、ツールバーに常時インジケータを出すか）。

## 10. 完了条件（受け入れ基準）

- Chrome / Edge で、データメニューからバックアップ先フォルダを選択できる。
- タスクを変更すると、数秒以内に指定フォルダの `worklist3.json` が最新内容へ更新される（一時ファイル経由の原子的書き込み）。
- `backups/` に `worklist3-YYYY-MM-DD.json` が作られ、既定世代（例7日）より古いものが自動削除される。
- localStorage を手動でクリアした後、`worklist3.json`（または `backups/` 内のファイル）を既存インポートで読み込むと、全タスクが復元される。
- タスクが0件／急減した状態では、バックアップが自動上書きされず、ユーザーへ通知される（破損伝播が起きない）。
- ページ再読み込み後もバックアップ先が復元される（権限再取得が必要な場合は再接続を促す）。
- フォルダ未接続でもアプリが従来どおり動作する。

---

## 付録：今回スコープ外とした案（検討経緯の記録）

将来の検討用に、今回あえて見送った案を残す。**現時点では実装しない。**

- **複数PCでの同期・共有**：バックアップ先フォルダを介してPC間でデータを同期する構想。ただし「ファイル丸ごとで新しい方を採用」すると、別々のPionでの編集が相互に消し合う（サイレントな全消失）リスクがあり、当初目的と逆行する。実装するなら**タスク単位マージ（`id` で突合し `updatedAt` の新しい方を採用）＋削除の tombstone 化＋原子的書き込み**が前提。単一ユーザー用途なら中リスクで実現可能だが、別テーマとして切り出す。
- **主保存の IndexedDB 化**：localStorage の容量上限（約5MB。30件/日で約2年弱、約11,000件で到達）への対策。既存の `TaskRepository` 抽象のおかげで差し替え自体は小規模（load/save の非同期化が主）だが、当面の課題（削除・破損による消失）には不要のため見送り。上限が現実味を帯びたら、次項のアーカイブと併せて検討する。
- **古い完了タスクのアーカイブ**：アクティブ＋直近のみをホットな作業セットとして保持し、古い完了タスクを年次アーカイブファイル（例 `archive/worklist3-2026.json`）へ退避。容量問題を最小コストで解消でき、アーカイブ自体がバックアップも兼ねる。容量が課題化した段階で導入する。
- **大規模スナップショット（時間ごと・14日=約112世代）**：誤操作の時点復元まで手厚く救う案。今回は「削除・破損での全消失」に絞ったため、§4.4 の少数日次ローテーションに縮小した。誤操作復元の要望が強まれば拡張を検討する。
