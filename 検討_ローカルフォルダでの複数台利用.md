# worklist3 検討書：ローカルフォルダ保存先での複数台利用（Issue #102）

> 本書は**実装するかどうかを決めるための検討書**であり、変更仕様書ではない。
> 対象は Issue #102「ローカルフォルダバックアップでも google drive と同様の切り替え機能実装」。
> 既存の `変更仕様書_複数台利用.md`（#91、手番制＝バトンパス）を、保存先「ローカルフォルダ」（File System Access API。以下 FSA）へ広げられるかを評価する。

## 0. 結論（先に書く）

**実用性スコア：67/100（合格ライン 70）。**射程は PC - PC に確定（§4-1 で実機確認）。スマートフォンを含めた場合の 55/100 は参考値。
**現時点ではどちらも合格に届かない。ただし不合格の理由は「使う同期フォルダの実際の挙動が分かっていない」ことにほぼ尽きる。**

要点は3つ。順に §5 以降で示す。

1. **技術的な射程は「PC - PC」まで（実機で確認済み。§4-1）。** FSA は Chromium 系デスクトップのみで、スマートフォンのブラウザにはこの API が無い（`fsa.ts:100`）。**ファイルがスマートフォンへ同期されて見えていても、ブラウザがそのフォルダを掴めない**ため、スマートフォン側では自動バックアップも手番の読み書きも成立しない。スマートフォンの Edge で保存先「ローカルフォルダ」が選べないことを実機で確認したので、**射程は PC - PC に確定**した（§9 の 55点の列は参考値）。
2. **手番制が拠って立つ前提（1つの正本を、両端末が同じタイミングで読める）が、同期フォルダでは成立しない。** 各端末が見るのは自分のローカル複製で、同期の到達を**アプリからは知る手段がない**。書き込みはローカルなので**常に成功する**ため、「相手に届いていない」ことに気づけない（§5 R3）。ここが減点の本体。
3. **その減点は「複製型の同期クライアント（遅延は分単位、競合すると別名のコピーが黙って作られる）」を仮定して置いている。** 実際に使う同期フォルダがそうでない場合——実体をサーバ側に置き、更新が秒単位で見え、競合をサーバ側のバージョンで処理する方式——なら、**同じ設計のまま 78/100 まで上がり合格する**（§9-3）。**したがって次にやるべきは §10 の実測であって、机上の判断ではない。**

なお **「手番制を入れない」は「今のままでよい」ではない。** 現状でも2台の PC が同じ同期フォルダを指すと、**互いのミラーを黙って上書きし合う**（ファイル名にグループ相当の区別が無い。`fsa.ts:36`）。ここは手番制と独立に、**安全柵だけ先に入れる価値がある**（§8 案1、実装量は小さい）。

## 1. Issue #102 が求めていること

| 求められたこと | 本書の対応箇所 |
|---|---|
| 機能面で不足がないかを確認 | §4 |
| ローカルフォルダ保存特有のリスクを確認 | §5 |
| 不足・リスク対策を検討 | §6・§7 |
| 実装でカバーできる範囲と運用で考慮すべきところの整理 | §6 の表 |
| 実用に耐えるかの点数化 | §9 |
| 実装するかの判断 | §0・§10 |

## 2. 現状の整理

### 2-1. 手番制（#91）は Drive 専用として作られている

仕様書 §5（技術メモ）が明示している。

> 手番ファイルの読み書きは `BackupTarget` に生やさず、**Drive 固有の機能として `gdrive.ts` に置く**。
> FSA では手番制を提供しないため、インターフェースを汚さない。

実装もそのとおりで、`current.id === "gdrive"` の分岐が要所に置かれている。

- `backup.ts:260` … 書き込み直前の降格検知（`ensureStillOwner`）は Drive 以外では素通り
- `backup.ts:630` … `batonAvailable()` が Drive 接続時のみ true
- `backup.ts:686` … フォルダIDの表示（接続先の同一性確認）も Drive のみ
- `Toolbar.tsx:604` … グループ名・端末名・「複数台で使う」トグルの UI ブロックごと Drive 限定

そして仕様書 §8 は、FSA を対象外にした理由をこう書いている。

> **FSA（ローカル同期フォルダ）は対象外。** 同期遅延が分単位あり、競合時には同期クライアントが別名のコピーを作るため、手番ファイルの一意性が保証できない。

**#102 は、この判断を覆せるかどうかの問いである。** 本書は「覆せない。ただし別の形で価値は出せる」と結論する。

### 2-2. FSA 保存先の現在のかたち

| 項目 | 現状 | 該当 |
|---|---|---|
| ミラー | フォルダ直下 `worklist3.json`（既定は非圧縮） | `fsa.ts:36`, `types.ts` `mirrorFileName` |
| 日次コピー | `backups/worklist3-YYYY-MM-DD.json` | `fsa.ts:46` |
| 接頭辞 | **固定の `worklist3`。端末名もグループ名も入らない** | `fsa.ts:36` |
| デバウンス | 1500ms（ローカル前提で短い） | `fsa.ts:106` |
| 書き込み | `createWritable()`＋`close()`（ローカルには原子的） | `fsa.ts:207` |
| 接続の記憶 | ディレクトリハンドルを IndexedDB に保持 | `fsa.ts` `idbRead/idbWrite` |
| 起動時の復帰 | **非対話。権限が `granted` でなければ未接続のまま** | `fsa.ts:182` |
| 対応環境 | `window.showDirectoryPicker` があること＝Chromium 系デスクトップ | `fsa.ts:100` |

**Drive の接頭辞にはグループ名が入る（`gdrive.ts:135`）が、FSA には無い。** つまり FSA では「フォルダ＝データ1本」であり、同じフォルダを2台が指した時点で、**すでにファイル名は共有されている**。分離する手段が無い代わりに、**潰し合いを止める仕組みも無い**。

## 3. Drive で成立していて、ローカルフォルダで消える前提

手番制は「排他ロック」ではなく「バトンパス」なので CAS を要求しない（仕様書 §3-1）。それでも、**次の3つの前提には依存している**。

| # | Drive での前提 | 同期フォルダでの実際 |
|---|---|---|
| P1 | 正本は1つ。書けば次の読み手にはその版が見える | 正本は N 個の複製。**到達までに分単位の遅延**があり、到達したかを知る手段が無い |
| P2 | 書き込みの成否が返る（失敗＝相手にも届いていない） | **ローカル書き込みは必ず成功する。** 同期クライアントが止まっていても成功に見える |
| P3 | 同名ファイルは1つ。上書きは上書き | 競合時に**同期クライアントが別名の複製を作る**（名前の規則はクライアント依存） |

手番制の4手順（①ミラー取得 ②サニティガード ③丸ごと置換 ④手番ファイル更新。仕様書 §4.5）のうち、**①が「相手の最新」である保証を与えていたのが P1**である。P1 が消えると、**古いミラーで手元を置換し、そのうえで手番を取る**——つまり「系譜は常に1本」という唯一の不変条件が、**利用者に気づかれないまま破れる**。

これが FSA 対応の中心的な難所であり、対策（§7 の M1）はここを狙う。

## 4. 機能面の不足（そのまま有効化した場合に足りないもの）

Drive 版と同じことをやろうとして、FSA に存在しないもの。

| # | 不足 | 内容 | 重さ |
|---|---|---|---|
| F1 | **スマートフォンのブラウザで使えない（実機で確認済み。§4-1）** | FSA は Chromium 系デスクトップのみ（`fsa.ts:100`）。フォルダが同期でスマートフォンへ届いていても、**ブラウザがそのフォルダを掴めない**ため、手番制も自動バックアップも成立しない。成立するのは PC-PC | **確定（PC-PC のみが射程）** |
| F2 | **グループ名が無い** | FSA の接頭辞は固定（`fsa.ts:36`）。「同じフォルダ＝同じデータ」で運用するなら不要とも言えるが、Drive と UI・概念がずれる | 中 |
| F3 | **手番ファイルの読み書き API が無い** | `readOwner` / `writeOwner` / `clearOwner` / `isEmptyGroup` / `writeRescue` / `readMirror`（本文＋最終更新時刻）はすべて `gdrive.ts:467-548` にある Drive 固有実装。FSA に同等が要る | 中 |
| F4 | **抽象の穴** | `backup.ts:260,630,686` と `Toolbar.tsx:604` が `gdrive` 直書き。保存先共通の機能にするなら、`BackupTarget` とは別の任意インターフェース（例 `BatonCapable`）へ切り出す設計変更が要る | 中 |
| F5 | **接続先の同一性を示す値が無い** | Drive はフォルダID を見せて2台の一致を確認させる（仕様書 §4.0a、`backup.ts:686`）。FSA のディレクトリハンドルに安定した公開 ID は無い。→ 対策 M3（フォルダに印のファイルを置く）で代替可能 | 小 |
| F6 | **最終バックアップ時刻の信頼度が落ちる** | バナーは「最終バックアップ: 絶対時刻＋相対時刻」を出す（仕様書 §4.4）。FSA では `File.lastModified` で取れるが、**それは自分の複製の更新時刻**であり、相手が書いた時刻でも、相手の変更が届いた時刻でもない場合がある | 中 |
| F7 | **起動直後は接続されていないことがある** | 権限の再要求はユーザー操作起点でなければ弾かれるため、`restore()` は非対話で失敗し得る（`fsa.ts:182`）。手番の確認が走らない時間帯が生まれる | 小 |

### 4-1. F1 の確認（**確認済み**）

本書の射程を決める前提なので、実機で確かめた。

- 確認日：2026-09-03
- 方法：スマートフォンの Edge で worklist3 を開き、💾メニューの保存先を見た
- 結果：**「ローカルフォルダ」を選べない。** `supported=false`（`fsa.ts:100`）と判定され、UI では選択不可になる

したがって **F1 は確定**であり、本書が評価する範囲は **PC - PC のみ**である。§9 の「スマートフォンも含める（55点）」列は、以後は参考値として残す。
スマートフォンを一本化に含めたいのであれば、道は Drive 保存先（実装済み・手番制がそのまま効く）か、ファイル選択による手動インポート（`Toolbar.tsx:822`）のどちらかになる。

さらに、**#102 とは独立に現状の不具合が1件見つかった**（§11）。

## 5. ローカルフォルダ保存に固有のリスク

| # | リスク | 何が起きるか | 深刻度 |
|---|---|---|---|
| R1 | **同期遅延** | 手番ファイルもミラーも、相手に届くまで分単位。省電力・スリープ・ファイルオンデマンドでさらに延びる | 高 |
| R2 | **競合コピー** | 両端末が同名ファイルを更新すると、同期クライアントが別名の複製を作る（OneDrive は端末名付き、Dropbox は "conflicted copy"、その他クライアントごとに規則が違う）。**この名前は `dailyFileDate()` の規約に一致しない**ため、アプリからは見えず、ローテーション掃除の対象にもならない＝**黙って片方の版が失われ、誰も気づかない** | **高** |
| R3 | **書けたことが届いたことを意味しない** | ローカル書き込みは常に成功する。同期クライアントが停止・未ログイン・容量超過でも、アプリの表示は「バックアップしました」のまま | **高** |
| R4 | **降格検知が遅れる** | 書き込み直前の手番確認（`backup.ts:259`）は**自分のローカル複製**を読む。相手が手番を取ったことは分単位で遅れて届くため、その間ずっと「自分が手番だ」と信じてミラーを上書きし続ける。Drive では起きない | **高** |
| R5 | **オフライン編集との噛み合わせ** | 相手が圏外・持ち出し中でも、こちらの奪取は成立する（禁止しない設計）。相手が戻ってきたときに R4 と R2 が同時に起きる | 高 |
| R6 | **同期クライアントの挙動差** | 競合コピーの命名、大文字小文字、ロック中ファイルの扱い、`.crswap` 一時ファイルの同期可否がクライアントごとに違う。**アプリ側でテストしきれない** | 中 |
| R7 | **書き込み頻度** | FSA のデバウンスは 1500ms（`fsa.ts:106`）。1台運用なら妥当だが、共有すると編集のたびに同期が走り、R2 の窓を自分で広げる | 中 |
| R8 | **世代管理の前提のずれ** | 自動バックアップは「クラウド側のバージョン履歴が世代管理を上乗せする」ことを狙っている（`変更仕様書_自動バックアップ.md` §2）。2台が同じファイルを書くと、その履歴は「どちらの版か」が混ざった列になり、復元時の判断材料として弱くなる | 中 |

**この表は「複製型の同期クライアント」を前提に置いている。** R1・R2・R6 の重さは、使う同期フォルダの方式によって変わる（§9-3・§10）。R3・R4・R5・R7 は方式によらず残る。

**R2・R3・R4 が同時に効くと、「サイレントなデータ喪失」という、このプロジェクトが一貫して避けてきた事故がそのまま起きる。** 手番制を FSA に載せる設計は、まずここを潰せるかで決まる。

## 6. 実装でカバーできる範囲 / 運用に残る範囲

| リスク・不足 | 実装でカバー | 運用で残る |
|---|---|---|
| F1 スマートフォンのブラウザで使えない | ✗ 不可能 | スマートフォンを一本化に含めるなら Drive 保存先を使う。手動でよければ、インポートはファイル選択（`Toolbar.tsx:822`）なのでスマートフォンからも同期フォルダのファイルを選べる |
| F2 グループ名 | ○ 「フォルダ＝1データ」と割り切れば不要 | — |
| F3/F4 API と抽象 | ○ 実装作業（中規模） | — |
| F5 接続先の同一性 | ○ M3（フォルダ印ファイル） | 印の値を2台で見比べる操作 |
| F6 最終バックアップ時刻 | △ M1 の版スタンプで代替（時刻より版の一致を見せる） | 「◯分前」の信頼度は下がると理解して使う |
| F7 起動直後の未接続 | ○ 未接続中は手番不明として編集を止めない（既存方針と同じ） | — |
| R1 同期遅延 | △ M1（版ゲート）で**遅延を「待ち」に変換**できる。遅延そのものは消せない | 受け渡し時に同期完了を待つ |
| R2 競合コピー | △ M4（競合コピーの検出と通知）で**気づけるようにはできる。復旧は人手** | 見つかったコピーの中身を人が確認して取り込む |
| R3 届いたか分からない | ○ M1 の版ゲートで「相手が受け取れる状態か」は判定できる | 同期クライアントが動いていることの確認 |
| R4 降格検知の遅れ | △ M2（渡す側からの譲渡を主経路にする）で大半は消せる。**受け側からの奪取では残る** | 「渡してから離席する」運用 |
| R5 オフライン編集 | ○ 既存の救出ファイル（仕様書 §4.6）がそのまま効く | 救出ファイルを人が読み戻す |
| R6 クライアント差 | ✗ | 使う同期クライアントを固定し、§10 の実測で確認 |
| R7 書き込み頻度 | ○ M6（共有ON時はデバウンスを延ばす） | — |
| R8 世代管理 | △ M5（版スタンプをファイルにも残す） | — |

**「気づけるようにする」までは実装で届く。「壊れない」までは届かない。** これが、複製型の同期フォルダを前提にしたときの FSA の限界である。

## 7. 対策案（実装するとした場合の骨子）

| # | 対策 | 何を解くか |
|---|---|---|
| **M1** | **版スタンプによる受け渡しゲート。** ミラーを書いた端末は、その内容の SHA-256（`crypto.subtle.digest`）・件数・連番・書いた時刻を手番ファイルへ記録する。受け側は**手番ファイルの版と、自分が読めたミラーの実ハッシュが一致したときだけ**置換と手番取得へ進む。不一致なら「相手の最新がまだ届いていません（同期待ち）」を出して待たせる | R1・R3・P1。**古いミラーでの置換という静かな事故を、目に見える「待ち」へ変換する**（本対策の中心） |
| **M2** | **譲渡（渡す側の操作）を主経路にする。** 渡す側が「この端末での更新を終える」を押すと、①保留中の変更を書き切る ②版スタンプを更新 ③自分を即座にローカル降格。受け側からの奪取は残すが、警告を強める | R4。降格の伝播を待たずに、渡す側で確定させる |
| **M3** | **フォルダ印ファイル。** 初回接続時に `worklist3-folder.json`（UUID）を置き、💾メニューに表示する。Drive のフォルダID 表示（仕様書 §4.0a）の代替 | F5 |
| **M4** | **競合コピーの検出。** フォルダ内を走査し、`worklist3` で始まるのに命名規約（ミラー／日次／救出／印）に一致しない名前があれば警告する | R2 |
| **M5** | **版スタンプをミラー本体とは別のサイドカーに置く。** ミラーはタスク配列そのままの JSON で、そのままインポートできることが価値なので、形式は変えない | R8・互換性 |
| **M6** | **共有ONのときはデバウンスを延ばす**（1.5秒 → 30秒程度）。同期クライアントが追随する余地を作る | R7・R2 |
| **M7** | **救出ファイルの流用。** 降格検知時の救出（仕様書 §4.6）は保存先非依存にできる | R5 |
| **M8** | **「同期クライアントが動いているか」は確認できないと明示する。** 💾メニューに一文置く | R3（残余の可視化） |

M1 が肝である。**これが無い FSA 手番制は、作ってはいけない。**

実装規模の見積り：`types.ts` へ任意インターフェース追加、`fsa.ts` に手番・版スタンプ・競合検出（＋120行程度）、`backup.ts` の `gdrive` 直書き4箇所の解消、`Toolbar.tsx` の UI 共通化、待ちダイアログの新規追加、純粋関数（版一致判定）の単体テスト。**おおむね 400〜600行規模の変更**で、Drive 側にも手を入れることになる（＝既存の動いている機構への波及がある）。

## 8. 選択肢

| 案 | 内容 | 実装量 |
|---|---|---|
| **案0** | 現状維持。何もしない | なし |
| **案1** | **安全柵のみ。** M3（フォルダ印）＋M4（競合コピー検出）＋「このフォルダは他の端末でも使われています」の警告＋💾メニューの注意書き。**手番制は入れない** | 小（100行前後） |
| **案2** | **FSA 手番制のフル実装。** M1〜M8 一式 | 中〜大（400〜600行） |
| **案3** | **運用で埋める。** ①一本化したい端末を**両方 Drive 保存先にする**（実装済み・手番制がそのまま効く） ②ファイル経由なら、既存のエクスポート／インポート（`mode: "replace"`）で明示的に渡す。インポートはファイル選択なので、スマートフォンからも同期フォルダのファイルを選べる ③保存先を分けたまま、端末ごとに別データとして使う（現状） |

## 9. 点数化

実装したとして**実運用に耐えるか**を測る。コストは §8 に別建てし、ここでは実用性のみを見る。**Drive 版の手番制（すでに動いていて、受け入れられている）を参照点**に置いて較正する。

### 9-1. 採点

一本化の対象にスマートフォンを含めるかで、「対象ユースケースの充足」だけが変わる。他の軸は同じ。

| 評価軸 | 重み | 案2：PC-PC のみ | 案2：スマートフォンも含める | 参照：Drive手番制 |
|---|---:|---:|---:|---:|
| 系譜が壊れない確度 | 30 | 3/5 → **18** | 3/5 → 18 | 5/5 → 30 |
| 壊れたときに気づける（サイレント喪失の無さ） | 20 | 4/5 → **16** | 4/5 → 16 | 5/5 → 20 |
| 対象ユースケースの充足 | 15 | 5/5 → **15** | 1/5 → 3 | 5/5 → 15 |
| 閉じ込めが起きない（#84 の再来を避ける） | 10 | 4/5 → **8** | 4/5 → 8 | 5/5 → 10 |
| 運用手順の簡単さ | 15 | 2/5 → **6** | 2/5 → 6 | 4/5 → 12 |
| 環境差異への頑健さ | 10 | 2/5 → **4** | 2/5 → 4 | 4/5 → 8 |
| **合計** | 100 | **67** | **55** | **95** |

### 9-2. 採点の根拠

- **系譜 3/5** … M1（版ゲート）で「古いミラーでの置換」は塞げる。しかし**受け側からの奪取**では、相手が手番喪失に気づくまで上書きの窓が残る（R4）。窓の幅は同期の遅延そのもの。
- **気づける 4/5** … M1 の不一致表示・M4 の競合コピー検出・救出ファイルで、静かな喪失はかなり減る。ただし競合コピーの命名が同期クライアント依存（R6）なので取りこぼしは残る。
- **充足** … PC-PC だけが要件なら、手番制はそのまま目的を果たす（5/5）。スマートフォンを含めるなら、F1 により**そちら側では機能自体が動かない**（1/5）。ここは技術的な可否の問題で、要件をどう置くかは判断する人が決める。
- **閉じ込め 4/5** … 奪取を禁止しない方針は維持できる。ただし M1 の「同期待ち」は、同期クライアントが止まっていると**永久に待ちになる**ため、明示確認つきの強制続行が別途必要。
- **運用 2/5** … 「渡してから離席する」「同期の到達を待つ」「競合コピーが出たら中身を人が確認する」を利用者に課す。1人用アプリとしては重い。
- **頑健さ 2/5** … 同期クライアントの実装差にそのまま晒される。アプリ側で検証しきれない（R6）。

### 9-3. 前提が変われば合格する

減点の大半（系譜・運用・頑健さ）は、**複製型の同期クライアント**——各PCにファイルの実体を複製し、到達に分単位を要し、競合すると別名のコピーを黙って作る——を仮定して置いた。

使う同期フォルダが**サーバ側に実体を置く方式**（更新が秒単位で相手から見え、競合をサーバのバージョン管理で処理する）なら、前提 P1〜P3 の破れ方が小さくなり、採点はこう変わる。

| 評価軸 | 重み | 複製型を仮定 | サーバ実体型だった場合 |
|---|---:|---:|---:|
| 系譜が壊れない確度 | 30 | 18 | 4/5 → **24** |
| 壊れたときに気づける | 20 | 16 | 4/5 → 16 |
| 対象ユースケースの充足（PC-PC） | 15 | 15 | 5/5 → 15 |
| 閉じ込めが起きない | 10 | 8 | 4/5 → 8 |
| 運用手順の簡単さ | 15 | 6 | 3/5 → **9** |
| 環境差異への頑健さ | 10 | 4 | 3/5 → **6** |
| **合計** | 100 | **67** | **78** |

**67 と 78 の差は設計の差ではなく、同期フォルダの挙動の差である。** どちらであるかは机上では決まらないので、§10 で実測する。

## 10. 判断と、再検討の条件

**判断：使う同期フォルダの挙動を実測するまで、案2（FSA 手番制）の着手は保留する。案1（安全柵）は手番制と独立に価値があるので、別 Issue として先に切り出す。**

実測は #91 §9（Drive の前提検証）と同じやり方で、**実際に使う同期フォルダで**行う。結果が下の条件を満たせば §9-3 の 78 点側に入るので、そのときは着手してよい。

1. **到達時間。** 小さなファイル（1KB 程度）を片方の PC で書き、もう片方の PC の FSA から新しい内容が見えるまでの時間を10回測る。**中央値が2分以内・最大が5分以内**。これを超えると受け渡しの待ち時間が実用にならない。
2. **競合時の挙動。** 両端末で同じファイルを同時に更新し、**別名のコピーが作られるならその名前を記録**する。M4 の検出規則がその名前を拾えることを確認する。サーバ側のバージョンに寄せられて別名コピーが作られないなら、R2 は消える。
3. **同期を止めたときの挙動。** 同期クライアントを一時停止した状態で、アプリが「バックアップしました」と表示し続けることを確認したうえで、M1 の待ち表示が正しく出ること。
4. ~~スマートフォンで保存先「ローカルフォルダ」を選べるか~~ → **確認済み（§4-1）。選べない。** 残る実測は 1〜3 の PC - PC のみ。

1〜3 のいずれかが崩れたら、そこで打ち切ってよい。

## 11. 検討中に見つかった既存の不具合（#102 とは独立）

**手番が無い（guest）状態のまま保存先をローカルフォルダへ切り替えると、読み取り専用から抜け出せなくなる。**

- `setBackupTarget()`（`backup.ts:488` 付近）は手番の状態を触らないため、`baton.enabled=true` / `role="guest"` が残る
- `refreshBaton()` は `batonAvailable()`（`backup.ts:630`＝Drive 接続時のみ true）で早期 return するため、状態は更新されない
- 編集と保存は `role === "guest"` で止まる（`App.tsx:279,336`）
- 解除用の「複数台で使う」トグルは **Drive 限定の UI ブロックの中**にある（`Toolbar.tsx:604`）ため、ローカルフォルダ表示では画面上に存在しない
- バナーの〔読み込んで、この端末で更新する〕は `planTakeover()` → 未接続の Drive を読みにいくため失敗する

結果、**Drive へ戻して再接続するまで編集できない。** #84 で潰したはずの閉じ込めと同じ形である。

修正は小さい（保存先の切り替え時に手番の状態を中立化する、あるいは手番トグルを保存先に依存しない位置へ出す）。**案1 とあわせて別 Issue で扱うのが妥当。**

## 12. 参照

- `変更仕様書_複数台利用.md`（#91）§3-1 バトンパス / §4.5 奪取の手順 / §8 FSA 対象外の理由 / §9 前提の検証
- `変更仕様書_バックアップ先の追加と圧縮.md` §1（Android に FSA が無い）
- `変更仕様書_自動バックアップ.md` §2（同期フォルダ前提とクラウド側の履歴）
- `src/lib/backupTargets/fsa.ts` / `src/lib/backupTargets/gdrive.ts` / `src/lib/baton.ts` / `src/lib/backup.ts`
