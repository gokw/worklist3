# worklist3 変更仕様書：Drive接続の維持(トークン永続化と先回り更新)

> 本書は Claude Code に実装を依頼するための変更仕様書です。Issue #96 に対応します。
> 既存の `変更仕様書_バックアップ先の追加と圧縮.md`・`変更仕様書_複数台利用.md` の決定事項を引き継ぎますが、
> **「アクセストークンはメモリ保持のみ」という過去の決定を、本書で明示的に上書きします**(§3-1)。

## 1. 症状(Issue #96)

- PC・スマートフォンとも Google ドライブとの接続がすぐ切れる
- PC は数分で切断され、💾メニューから再接続しないとならない
- スマートフォンは画面リロードのたびに何か接続画面を経由している感じがする

## 2. 原因の整理

### 2-1. 前提: GIS トークンモデルの制約

ブラウザだけで完結する GIS(Google Identity Services)のトークンモデルには、次の制約がある。

- **リフレッシュトークンは発行されない。** アクセストークンの寿命は約1時間で、延ばせない。
- **自動(無人)のトークン更新はライブラリが意図的にサポートしない。** 公式ガイドは
  「失効を検知したら、**ユーザー操作起点のイベントから** `requestAccessToken()` で取り直す」ことを
  アプリ側の責務としている(トークンモデルのガイドおよび GIS 移行ガイドに明記。§7 検証記録)。
- 取り直し(`requestAccessToken`)は**ポップアップを経由する**。`prompt: ""` の無音取得でも
  ポップアップ自体は開き(セッションと同意が有効なら即閉じる)、**ユーザー操作の外**
  (ページ読み込み直後・タイマー・デバウンス後の書き込み)から呼ぶと
  **ポップアップブロックで開けず失敗し得る**。開けなかった/閉じられた事象は
  `error_callback`(`popup_failed_to_open` / `popup_closed`)でしか捕捉できない。

### 2-2. 現在の実装が「切れる」経路

| 場面 | いま起きること | 根本原因 |
|---|---|---|
| リロード(スマホの典型) | トークンがメモリのみなので消える。起動時の `restore()` が無音取得を走らせ、認証画面を一瞬経由する。ブロックされれば未接続のまま | トークン非永続([googleAuth.ts](src/lib/googleAuth.ts) 冒頭の設計判断)+ 取得が画面経由 |
| タブ破棄からの復帰 | リロードと同じ。**PC ブラウザのメモリセーバー等は非アクティブなタブを破棄する**(設定によっては数分)ため、「PCは数分で切断」の体感と整合する | 同上 |
| 使用中に1時間の失効 | 30秒デバウンス後の書き込みが 401 →無音取り直し([gdrive.ts:184](src/lib/backupTargets/gdrive.ts))→ユーザー操作外なのでブロック→「権限が切れています。再接続してください」 | `expires_in` を読んでおらず失効を予見していない + 取り直しが操作外 |
| 無音取得がブロックされたとき | `initTokenClient` に `error_callback` を渡していないため callback が一度も呼ばれず、**`acquire()` の Promise が永遠に確定しない**。起動時なら接続処理が黙って止まる。**書き込み中なら [backup.ts:335](src/lib/backup.ts) の `flush()` が `writing = true` のまま二度と戻らず、以降のバックアップが警告も出ずに全停止する(デッドロック)**。最終成功時刻が止まったままになり「気づいたら切れていた」に見える | error_callback 未設定([googleAuth.ts:99](src/lib/googleAuth.ts)) |
| 💾メニューから再接続 | `prompt: "consent"` を毎回指定([googleAuth.ts:127](src/lib/googleAuth.ts))しているため、**再接続のたびにフル同意画面** | consent の強制 |

どの経路も最後は「再接続してください」に合流する。切断そのものより、
**復帰のすべてが画面経由・手動**であることがストレスの正体である。

## 3. 決定事項(設計方針)

### 3-1. アクセストークンを localStorage へ保持する(方針転換)

過去の仕様書は「トークンはメモリのみ。localStorage に置くのは機微でない値だけ」と定めた。
これを上書きし、**アクセストークンを有効期限つきで localStorage に保存する**。

- キー: `worklist3.gauth.<スコープ>`。値は `{ token, expiresAt, scopes }` の JSON。
- `GoogleTokenSource.current` は**期限内のときだけ**値を返す(期限切れは「無い」扱い)。
- `acquire()` 成功時に保存、`forget()` / `reset()`(切断)で必ず削除する。revoke は従来どおり。

**上書きの理由(安全性の再評価):**

- この app の**主保存データ(タスク全件)が同じ localStorage にある**。XSS が成立する前提では、
  攻撃者はトークンを盗むまでもなく Client ID を読んで自分でトークンフローを起こせるため、
  「トークンだけメモリに置く」は実質的な防御線になっていない。
- 置くのは寿命約1時間・`drive.file` スコープ(このアプリが作ったファイルのみ)のトークンで、被害面が小さい。
- 得られるものは大きい: **リロード・タブ破棄後の復帰が、GIS を一切呼ばずに完了する**。
  スマホの「リロードのたびに接続画面」、PC の「数分で切断」の主経路がこれで消える。

**補足(複数タブ):** localStorage はタブ間で共有されるため、複数タブが同じトークンを使う。
これは問題ではなく利点(どちらのタブも無音で接続済みになる)。401 時の `forget()` は
保存分も消すので、失効の伝播も自然に揃う。`storage` イベントでの同期までは行わない(過剰)。

あわせて [docs/security-check.md](docs/security-check.md) の該当記述(「アクセストークンはメモリ保持のみで永続化しない」)と、
[googleAuth.ts](src/lib/googleAuth.ts)・[gdrive.ts](src/lib/backupTargets/gdrive.ts)・[gcalClient.ts](src/lib/gcalClient.ts) の
ヘッダコメントを本方針に合わせて更新する。

### 3-2. `expires_in` を読んで失効を予見する

- `TokenResponse` に `expires_in?: number`(秒)を追加し、`expiresAt = 取得時刻 + expires_in - 60秒` を記録する
  (60秒は「取得直後に使い切れず失効」を避ける余裕)。
- これにより「失効したトークンで書き込んで 401 を食らってから気づく」が、
  「書き込む前に取り直しが要ると分かる」に変わる。401 時の1回リトライは保険として残す。

### 3-3. `error_callback` を設定して失敗を確定させる(デッドロック修正)

`initTokenClient` に `error_callback` を渡し、ポップアップが開けなかった(`popup_failed_to_open`)・
閉じられた(`popup_closed`)場合に `acquire()` の Promise を**必ず reject させる**。

これは単なる作法ではなく**不具合修正**である。現在は Promise が確定しないまま残るため、

- 起動時の `restore()` が黙って止まる(未接続のままバックアップが始まらない)
- 書き込み中の 401 →取り直しでハングすると、`flush()` の `writing` フラグが立ったままになり
  **以降のバックアップが警告なしに全停止する**([backup.ts:335](src/lib/backup.ts))

の2経路がある。加えて保険として、`acquire()` には**タイムアウト(60秒程度)**を持たせ、
error_callback が想定外に呼ばれないケースでも Promise が必ず確定するようにする
(タイムアウト時は失敗扱い。§3-5 の段階化に合流する)。

### 3-4. ユーザー操作の文脈で先回りして取り直す

無音取得はユーザー操作の外ではブロックされるが、**操作ハンドラの中なら開ける**
(かつ同意済み・Google セッションありなら画面は一瞬で閉じるか出ない)。これを利用する。

これは我流ではなく、**公式ガイドが指示する更新方法そのもの**である(「失効を検知したら
ユーザー操作起点のイベントから取り直す」。§7)。本変更は「検知してから」ではなく
「失効の少し前に」行うことで、失効の瞬間を利用者に見せない。

- Drive 接続中は、`document` の `pointerdown` / `keydown`(capture)を監視し、
  **トークンの残りが10分を切っていたら**その操作の文脈で無音取得を発火する。
- スロットル: 取得中は再発火しない。失敗したら5分は再試行しない(操作のたびに画面が
  瞬く事態を避ける)。
- 効果: 使い続けている限り1時間の壁を跨いでもトークンが常に新しく、
  デバウンス書き込みが失効に当たらない。**PC で作業中にバナーが出る経路が消える。**

**実装上の制約(ここを外すと成立しない):**

- ブラウザがポップアップを許すのは、ユーザー操作から続く**短い時間窓(透過的アクティベーション、
  概ね5秒)**の間だけ。ハンドラ内で `requestAccessToken` を呼ぶまでに **ネットワーク待ちや
  長い await を挟んではならない**。接続中は GIS スクリプトとトークンクライアントが初期化済み
  なので、この経路では同期的に呼べる(初期化待ちが要るのは接続時だけにする)。
- セッションと同意が有効でも、**ポップアップが一瞬開いて閉じる(フォーカスが揺れる)可能性はある**。
  発火は1時間に1回程度・クリック等の直後なので許容するが、入力欄へのタイプ中(`keydown`)は
  避けて `pointerdown` を優先するなど、体感を損ねない工夫をする。

### 3-5. 失効時に即「再接続してください」へ落とさない(段階化)

書き込み時に `ensureWritable()` が false でも、直ちに `needsReconnect` の警告にしない。

1. まず pending(未送信スナップショット)を保持したまま、**「次のユーザー操作で1度だけ取り直す」**を予約する。
2. 取り直せたら flush して何事もなかったことにする(オフライン復帰と同じ扱い)。
3. その取り直しも失敗した(= Google セッション自体が切れている・同意が取り消された)ときだけ、
   従来の「💾メニューから再接続してください」を出す。

localStorage が主保存である以上、控えの書き込みが数分遅れることは失うものではない。
警告は「人の操作でしか直らない」と確定してから出す。

### 3-6. 再接続で consent を強制しない

- `connect()` / `reconnect()` の対話的取得は `prompt` を**未指定**にし、必要な画面の選択を GIS に委ねる
  (同意済みの再接続ならアカウント確認程度で済む)。
- `prompt: "consent"` を使うのは、`missingRequestedScope`(同意画面でチェックを外された)を検知した後の
  再試行だけにする。スコープを付け直すには consent が必要という現在のコメントの知見は正しいので、そこに限定する。

## 4. 変更対象

| ファイル | 変更 |
|---|---|
| [src/lib/googleAuth.ts](src/lib/googleAuth.ts) | §3-1 永続化 / §3-2 expires_in / §3-3 error_callback。変更の中心 |
| [src/lib/backupTargets/gdrive.ts](src/lib/backupTargets/gdrive.ts) | §3-6 prompt の整理。`token()` は期限を考慮した cached 判定に |
| [src/lib/backup.ts](src/lib/backup.ts) | §3-4 操作時の先回り更新の設置 / §3-5 失効時の段階化 |
| [docs/security-check.md](docs/security-check.md) | トークン保存方針の記述を更新 |

- 方針ロジック(期限判定・段階化)は可能な範囲で純粋関数に切り出してテストする。
  GIS・fetch に触る部分は従来どおり自動テストの対象外。

## 5. 採らない案

| 案 | 理由 |
|---|---|
| 認可コードフロー + リフレッシュトークン | **完全な解決策はこれだけ**(リフレッシュトークンで真の無人更新ができ、ポップアップが一切消える)であることは認識しておく。ただし Google はブラウザ単体の公開クライアントにリフレッシュトークンを発行しないため、トークン交換を行うバックエンド(サーバレス関数でも可)と client_secret の管理が必須になる。静的ホスティングで完結する本アプリの前提に反するので今回は採らない。**本書の対策後もなおストレスが残る場合の、次の一手として保留** |
| トークン寿命の延長 | Google 側の仕様で不可(約1時間固定) |
| sessionStorage への保持 | タブを閉じる・PWA 再起動で消え、主症状(リロード・タブ破棄)の一部にしか効かない。主保存と同じ localStorage に揃える |
| タイマーによる定期自動更新 | ユーザー操作の外ではポップアップブロックで成立しない(§2-1)。操作起点の先回り(§3-4)で代替 |
| カレンダー連携への同時適用 | 📅ボタンというユーザー操作起点でしか取得しないため痛みが小さい。`GoogleTokenSource` へ載せ替えれば同じ恩恵を受けられるが、本書の対象外(将来課題) |

## 6. 受け入れ確認(手動)

1. Drive 接続後にリロード → **接続画面を一切経由せず**接続済みに戻る(トークン期限内)
2. PC でタブを長時間放置(破棄)して復帰 → 1. と同じ
3. 接続から1時間を超えて操作を続ける → バナーが出ず、バックアップが継続している
4. 1時間超放置してから戻り、何か操作する → 最初の操作で無音更新され、バナーが出ない
5. Google からログアウトした状態で書き込みが発生 → このときだけ「再接続してください」。
   再接続ボタンはフル同意画面ではなく軽い画面で済む
6. 💾メニューで切断 → localStorage にトークンが残っていない(開発者ツールで確認)
7. 同意画面で Drive のチェックを外して接続 → 従来どおりチェックを促すメッセージが出て、
   再試行では同意画面(consent)が出る
8. カレンダー連携(📅)が従来どおり動く
9. **デッドロック回帰確認**: ポップアップを全ブロックに設定した状態でトークンを失効させ、
   書き込みを発生させる → バックアップは止まるが、取得の失敗が(タイムアウト含め)確定して
   §3-5 の段階化に進み、ポップアップ許可後の次の操作で自動復帰する。
   `flush()` が固まって以降の書き込みが全停止しないこと

## 7. 検証記録(本仕様の根拠)

本仕様の前提は、Google 公式ドキュメントで以下のとおり確認した(2026-09 時点)。

| 前提 | 出典 |
|---|---|
| トークンモデルではリフレッシュトークンは発行されず、トークンはページ読み込み時またはユーザー操作起点の `requestAccessToken()` で取得する。**自動更新はライブラリが意図的に非サポート**で、失効の検知と取り直しはアプリの責務 | [Use the token model](https://developers.google.com/identity/oauth2/web/guides/use-token-model) |
| implicit(トークン)モードでは、トークン要求に**ユーザー操作が必要** | [Migrate to Google Identity Services](https://developers.google.com/identity/oauth2/web/guides/migration-to-gis) |
| `error_callback` はポップアップ起因の失敗(`popup_failed_to_open` / `popup_closed`)を受け取る。**設定しなければこれらの失敗は通知されない** | [JavaScript API reference](https://developers.google.com/identity/oauth2/web/reference/js-reference)・[Handle errors](https://developers.google.com/identity/oauth2/web/guides/error) |
| `TokenResponse` に `expires_in`(秒)が含まれる | 同上(JS リファレンス) |

コード側は以下を再検証した。

- `acquire()` が確定しない場合、[backup.ts:335](src/lib/backup.ts) の `flush()` が `writing = true` の
  まま戻らず、以降の `flush()` が早期 return で**バックアップが警告なしに全停止**する(§2-2、§3-3)。
- `gdrive.ts` の `token()` はキャッシュの期限を見ていないため、失効後の最初の書き込みは必ず
  401 を踏む(§3-2 で解消)。
- 30秒デバウンス後の書き込みは、直前のユーザー操作から見て透過的アクティベーション
  (約5秒)の外にあり、そこからの無音取得はポップアップブロックの対象になる(§3-4 の根拠)。
