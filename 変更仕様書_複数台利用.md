# worklist3 変更仕様書：複数台利用（更新端末の手番制）

> 本書は Claude Code に実装を依頼するための変更仕様書です。Issue #91 に対応します。
> 既存の `変更仕様書_自動バックアップ.md`・`変更仕様書_バックアップ先の追加と圧縮.md` の決定事項を引き継ぎます。
> **完全自動の同期は目的ではありません。**「更新してよい端末を人が指定し、渡すときに前の端末の成果を必ず取り込む」ことだけを実現します。

## 1. 目的

自宅PCとスマートフォンの2台で、**同じ1本のデータ**を使えるようにする。

現在、バックアップは一方向の書き込み専用で、Drive 上のファイルは端末名で分離されている
（`worklist3-<端末名>.json.gz`）。そのため2台を同じ Drive に向けても、**潰し合わない代わりに混ざらない**。
端末ごとに別々のデータが育つだけで、複数台利用にはなっていない。

本変更は、既存の窓単位の書き手ロック（#57）の考え方を**端末単位へ広げる**。
ただし後述のとおり、機構としては排他ロックではなく**バトンパス**を採る。

## 2. 潰す対象と、あえて対象外にするもの

| 課題 | 扱い | 方針 |
|---|---|---|
| 2台で同じデータを使えない | **対象** | 手番（更新してよい端末）を1台に定め、手番の受け渡し時に丸ごと読み込む |
| どちらが更新側か分からない | **対象** | 手番を持たない端末は読み取り専用にし、更新中の端末名と最終バックアップ時刻を明示する |
| 手番の受け渡しでデータを失う | **対象** | 受け渡しは「読み込んでから引き継ぐ」1操作にし、サニティガードを通す |
| 更新端末が到達不能で切り替えられない | **対象** | 奪取は決してブロックしない。事実を見せて明示確認を求めるだけ（#84 の閉じ込めを繰り返さない） |
| 3台以上での運用 | 対象外 | 仕組み上は動くが、想定は2台。UI もそれを前提に書く |
| 双方向マージ・tombstone | **対象外** | 系譜を1本に保つ設計なので不要。採らない（§3-1） |
| 常時同期・リアルタイム反映 | 対象外 | 目的ではない |
| FSA（ローカル同期フォルダ）での手番制 | **対象外** | 同期遅延が分単位で競合コピーも作られるため、機構が成立しない（§8） |

## 3. 決定事項（設計方針）

### 3-1. 機構は「排他ロック」ではなく「バトンパス」

守る不変条件はこれ1つ。

> **データの系譜は常に1本。**
> 端末Aで編集 →（ミラー）→ 端末Bが**丸ごと読み込んでから**編集 →（ミラー）→ 端末Aが**丸ごと読み込んでから**編集 …

「同時に書かせない」ではなく「**手番を受け取るときに、前の手番の成果を必ず取り込む**」を保証する。
系譜が1本なら2つの版を突き合わせる場面が存在しないため、以下がすべて**不要**になる。

- **compare-and-swap（CAS）**：Drive の `files.update` に If-Match 相当の前提条件は文書化されていない。
  排他ロックなら致命的だが、バトンパスでは不要。手番の移動は人の明示操作でしか起きず、
  利用者は1人なので「2台で同時に奪取ボタンを押す」は物理的に起こらない。
- **タスク単位マージ**：読み込みは丸ごと置換なので突合が発生しない。
- **削除の tombstone**：丸ごと置換なので削除もそのまま伝わる。
- **`updatedAt` の比較・端末間の時計ずれ対策**：版の新旧を機械判定しないため不要。

前仕様書の付録が「複数端末の同期にはマージ＋tombstone が前提」としていたのは、
**両端末が独立に書く**前提に立っていたため。手番制ではその前提自体が消える。

### 3-2. その他の決定

| 論点 | 決定 | 理由 |
|---|---|---|
| 手番の識別子 | **端末ID（`crypto.randomUUID()`）**。端末名は表示専用 | 端末名は利用者が手で打つ表示名で、重複を防げない。重複すると2台が同時に「手番あり」と判定し、両方が書く（§4.2） |
| 手番の記録場所 | Drive 上の小さな JSON `worklist3/worklist3-owner.json` | ミラー本体（数百KB）を落とさずに読める。更新もミラーと独立 |
| 「相手の最終バックアップ」 | Drive の `modifiedTime` をそのまま使う | 専用のフィールドを持つ必要がない。`files.list` で既に取得している情報 |
| 有効化 | **設定で明示的にON。既定はOFF** | 1台運用の利用者に無用な仕組みを背負わせない。Drive を選んだだけでは有効にしない |
| 奪取の可否 | **決してブロックしない。** 事実を見せて明示確認を求めるだけ | 相手のブラウザデータが消えた場合に永久に手番を取れなくなる。#84 の閉じ込めと同じ形 |
| 奪取の undo | **undo の対象外** | データだけ戻って手番ファイルが自分のまま残ると、系譜が1本という不変条件が破れる（§4.5） |
| 起動直後の表示 | ローカルにキャッシュした手番状態で**即座に描画**し、確認は裏で走らせる | 手番の確認はネットワーク往復。確認中に編集を止めると、タブを切り替えるたびに固まる |
| 降格時の未送信データ | **救出ファイルとして書き出す** | 上記キャッシュ方式と、圏外編集の両方の安全網を兼ねる（§4.6） |

## 4. 機能要件（詳細仕様）

### 4.1 手番ファイル

Drive の `worklist3` フォルダ直下に置く。

```json
{
  "deviceId": "9f2c...",
  "deviceName": "自宅PC",
  "since": "2026-08-31T09:12:03.000Z"
}
```

- `deviceId` … 手番の判定に使う唯一の値
- `deviceName` … 他端末のバナーに出す表示名のみ
- `since` … 手番を取得した時刻（表示用）

### 4.2 端末ID

- 初回に `crypto.randomUUID()` で生成し、`worklist3.device.id` として localStorage に保存する。
- **端末名（`worklist3.gdrive.device`）は識別子として使わない。** 表示のみ。
- 端末名を変更しても端末IDは変わらない（手番を持ったまま名前だけ変えられる）。
- ブラウザデータを消すと端末IDも消える。その端末は「別の端末」として扱われ、
  手番を取り直す必要がある。これは正しい挙動とする（データも消えているため）。

### 4.3 3つの状態

| 状態 | 判定 | ふるまい |
|---|---|---|
| **手番あり** | `owner.deviceId === 自分のID` | 通常どおり編集可。バックアップも書く |
| **手番なし** | `owner.deviceId !== 自分のID` | 読み取り専用。バナーを出す |
| **未設定** | 手番ファイルが無い | 自分を手番にして書き込む（初回） |

手番を持たない端末は、**localStorage への保存もバックアップの書き込みも一切行わない**。
既存の読み取り専用窓（#57）と同じ扱いで、`App.tsx` の保存 `useEffect` で止める。

### 4.4 手番なしのバナー

```
　この端末は読み取り専用です
　更新中の端末: 自宅PC
　その端末の最終バックアップ: 2026/08/31 09:12（3分前）／431件
　いまの手元: 428件（最終更新 8/30 21:00）

　〔自宅PC のデータを読み込んで、この端末で更新する〕
```

- 相対時刻（「3分前」）は端末の時計に依存するため、**絶対時刻も必ず併記する**。
- 件数は `readMirrorCount()` で取得する。取得できなければ「不明」と出し、奪取は妨げない。

### 4.5 手番の奪取（受け渡し）

ボタンは「ロックを奪う」ではなく「**読み込んでから引き継ぐ**」1操作。以下の順に行い、
**②③を通らずに④だけ起きる経路を作ってはならない。**

1. 相手のミラーを取得する
2. **サニティガードを通す**（§4.7）。引っかかったら理由を見せて明示確認を求める
3. 手元のタスクを**丸ごと置換**する（既存の「すべてリセットして読み込む」と同じ経路）
4. 手番ファイルを自分に書き換える

- **undo は積まない。** 確認ダイアログに「この操作は元に戻せません」と明示する。
- 最終バックアップが古い場合は、確認ダイアログで平易に危険を述べる。
  例：「自宅PC は6時間前から更新されていません。その端末に未送信の変更が残っている可能性があります。
  読み込むとそれらは失われます。」
- **相手の状態がどうであれ、奪取自体は実行できる。** 警告は出すが禁止はしない。

### 4.6 降格と救出ファイル

「自分は手番を持っている」と信じていた端末が、そうでないと分かったときの処理。

**検知のタイミング**
1. バックアップの書き込み直前
2. タブの再表示時（`visibilitychange`）
3. 起動時のキャッシュ確認

**手番を失っていた場合**

1. ミラーではなく `worklist3/worklist3-<端末名>-救出-YYYY-MM-DDTHHmm.json.gz` として書き出す
2. 読み取り専用へ落とし、利用者に伝える
   「他の端末が更新側になりました。この端末の変更は救出ファイルとして保存しました。」
3. 救出ファイルは既存のインポート経路でそのまま読み戻せる

- 降格を検知できるのは**手番ファイルを読めたとき＝オンラインのとき**なので、救出ファイルは必ず書ける。
- 判定を複雑にしないため、**降格時は常に書く**（失うものが無い場合も書かれるが、稀な事象なので許容）。
- 救出ファイル名は日次コピーの命名規約
  （`<prefix>-YYYY-MM-DD.json(.gz)`）に**一致しない**ため、ローテーション掃除の対象外になる。
  溜まったら利用者が手で消す。§8 に明記する。

### 4.7 サニティガードの適用範囲を広げる

現在 `guardReason()` は `backup.ts` の `writeBackup()` からしか呼ばれておらず、
**読み込み側には一切かかっていない。**

奪取は「相手のミラーで手元を置換する」操作なので、相手のデータが壊れて激減していると
**手元を先に失う**（その後の書き込みはガードが止めるが、手元はもう消えている）。

- **奪取の置換前に** `guardReason(相手の件数, 自分の件数)` を適用する。
- 既存の純粋関数をそのまま使う。閾値（`GUARD_DROP_RATIO` / `GUARD_DROP_MIN`）も共通。
- 引っかかった場合は理由を見せ、明示確認があれば続行できる（禁止はしない）。

### 4.8 起動時・復帰時のふるまい

- 手番の状態を `worklist3.baton.owned` / `worklist3.baton.ownerName` として localStorage にキャッシュする。
- **起動時はキャッシュで即座に描画し**、手番の確認は裏で走らせる。
  手番の移動は人の明示操作でしか起きないため、キャッシュはほぼ常に正しい。
- **確認中に編集を止めない。** 食い違いは §4.6 の救出ファイルで拾う。
- 手番ファイルを読めなかったとき（オフライン・トークン切れ）は**キャッシュを正とする**。
  ここで悲観的に読み取り専用へ落とすと、圏外のスマートフォンで打刻できなくなり、#85 / #86 が死ぬ。

### 4.9 読み取り専用の二重化を整理する

窓ロック（#57）の読み取り専用と、手番なしの読み取り専用は同時に起こり得る。
**#57 のバナーには〔この窓で編集〕があるが、端末に手番が無ければ押しても編集可にならない**（新しい閉じ込め）。

- **手番なしを優先して表示する。** 〔この端末で更新する〕だけを出し、〔この窓で編集〕は出さない。
- 手番があり窓が secondary のときだけ、従来どおり #57 のバナーを出す。
- 書き手の条件は2層の論理積：**手番を持つ端末の、書き手の窓**だけが唯一の書き手。

## 5. 実装方針（技術メモ）

- 手番の判定・状態遷移は**純粋関数へ切り出して単体テストの対象にする**
  （`guardReason` / `expiredDailyDates` と同じ方針）。Drive とのやり取りはテスト対象外。
- 手番ファイルの読み書きは `BackupTarget` に生やさず、**Drive 固有の機能として `gdrive.ts` に置く**。
  FSA では手番制を提供しないため、インターフェースを汚さない。
- 奪取の「丸ごと置換」は、既存のインポート `mode: "replace"` の経路を再利用する
  （`pushUndo` を積まない点だけ変える）。
- 手番の確認は手番ファイル1本の取得のみ。ミラー本体は奪取のときだけ落とす。

## 6. UI変更点

- **💾 メニュー**：「複数台で使う（更新端末を指定する）」のON/OFF。ONのときだけ以下を出す。
  - 現在の手番: この端末 / 自宅PC / 未設定
  - 手番を持っているときは〔手番を手放す〕（任意。他端末が奪取できるので必須ではない）
- **手番なしのバナー**（§4.4）。既存の #57 バナーと同じ位置に出す。
- **降格の通知**（§4.6）。トーストではなくダイアログで出す（見逃すと救出ファイルの存在に気づけないため）。

## 7. ファイル別・変更概要

| ファイル | 変更 |
|---|---|
| `src/lib/baton.ts` | **新規**。手番の状態・遷移の純粋ロジック、localStorage キャッシュ、状態通知 |
| `src/lib/baton.test.ts` | **新規**。状態遷移・キャッシュ優先・ガード適用の単体テスト |
| `src/lib/backupTargets/gdrive.ts` | 手番ファイルの読み書き、ミラーの取得、救出ファイルの書き出し |
| `src/lib/backup.ts` | 書き込み直前の手番確認と降格。`guardReason` は変更しない |
| `src/components/BatonBanner.tsx` | **新規**。手番なしのバナーと奪取ダイアログ |
| `src/components/Toolbar.tsx` | 💾 メニューに手番制のON/OFFと状態表示 |
| `src/App.tsx` | 保存 `useEffect` の条件に手番を追加。読み取り専用バナーの優先順位（§4.9） |
| `README.md` / 本仕様書 | 記述の更新 |

## 8. 非機能・エッジケース

- **【許容する限界】バックアップ未達の編集は取りこぼす。** Drive のデバウンスは30秒。
  編集直後にタブが強制終了されればミラーに乗らない。救出ファイル（§4.6）は
  「降格に気づけた場合」しか働かず、そのブラウザが二度と開かれなければ何も起きない。
  これは「バックアップを介して手番を渡す」方式に内在する限界で、消すには常時同期かマージが要る。
  **本変更ではこれを受け入れ、最終バックアップの時刻と件数を見せて人が判断する。**
- **オフラインでは奪取できない。** 相手のミラーを読めないため安全な引き継ぎが成立しない。
  運用でカバーする＝**持ち出す端末に、出かける前（自宅Wi-Fi内）で手番を渡す**。
- **FSA（ローカル同期フォルダ）は対象外。** 同期遅延が分単位あり、競合時には同期クライアントが
  別名のコピーを作るため、手番ファイルの一意性が保証できない。会社PCのローカルフォルダ運用は従来どおり。
- **救出ファイルは自動で消えない。** ローテーションの命名規約に一致しないため掃除対象外。溜まったら手で消す。
- **手番ファイルを手で消した場合**：「未設定」に戻り、次に開いた端末が手番を取る。
- **3台以上**：仕組みとしては動くが、バナーは「更新中の端末」を1つ出すだけで、想定は2台。
- **端末IDの重複**：`crypto.randomUUID()` なので実質起こらない。
- **手番制OFFのとき**：現在と完全に同じ挙動（端末名でファイルが分かれ、各端末が独立に書く）。

## 9. 完了条件（受け入れ基準）

1. 手番制OFFのとき、現在と挙動が変わらないこと。
2. 端末Aで手番を取り、端末Bで開くと読み取り専用になり、Aの端末名と最終バックアップ時刻・件数が出ること。
3. 端末Bで奪取すると、**Aのデータが読み込まれてから**Bが編集可になること。
4. 奪取後にAのタブへ戻ると、Aが降格し、救出ファイルが書かれ、ダイアログで知らされること。
5. 相手のミラーが激減しているとき、奪取の置換**前**にサニティガードが理由を出すこと。明示確認で続行できること。
6. 相手の最終バックアップが古くても、**奪取が禁止されないこと**（警告のみ）。
7. オフラインで手番ファイルを読めないとき、手番を持つ端末が読み取り専用に落ちないこと。
8. 手番の無い端末が localStorage にもバックアップにも書かないこと。
9. 端末名を2台で同じにしても、端末IDで正しく区別されること。
10. 手番なしのとき、#57 の〔この窓で編集〕が出ないこと。
11. 奪取が `Ctrl+Z` で取り消せないこと。
12. 単体テスト・`tsc --noEmit`・`npm run build` が通ること。実ブラウザで 2〜11 を確認すること。

## 付録：採らなかった案（検討経緯の記録）

- **端末間の排他ロック**：手番ファイルを mutex として扱う案。2つの理由で採らない。
  ①Drive に CAS が無く、取得を原子的にできないため競合窓が残る。
  ②**到達できない端末がロックを持ったまま**になると（スマートフォンが圏外・電源断）、
  もう一方が永久に読み取り専用になる。#84 で潰した閉じ込めと同じ形。
  TTL と強制奪取を入れれば②は防げるが、その瞬間ロックは排他の保証ではなく助言に落ち、
  結局マージが必要になる。**ロックは安全装置になれない。**
- **双方向の自動マージ＋tombstone**：正しい同期。ただし §3-1 のとおり、手番制では不要。
  完全自動を求めるようになった場合の将来案として残す。
- **追記のみの片方向同期**：スマートフォンで新規作成した記録だけをPCへ流す案。衝突が原理的に起きず安全だが、
  **PCで立てた予定をスマートフォンで打刻する**流れを拾えないため、実際の使い方に合わない。
- **最終更新時刻による自動選択**：新しい方を自動採用する案。別々の端末での編集が相互に消し合う
  （前仕様書の付録が指摘したとおり）。人の明示操作を挟む本方式で置き換える。
