# worklist3 変更仕様書：モバイル対応（スマートフォンでの打刻と一覧）

> 本書は Claude Code に実装を依頼するための変更仕様書です。
> `変更仕様書_バックアップ先の追加と圧縮.md` で「別テーマ」として切り出したモバイル向け表示を扱います。

## 1. 目的

worklist3 は「キーボードで速く、Excel 並みの密度で、自分の1日を分単位で計測する」ことに全振りした設計で、
`要件.md` にも「個人利用。PCブラウザから、業務中に頻繁に開閉して使う」と明記されている。
一方でいま、**家庭でのライフログ用途としてスマートフォンから使いたい**という要求が出ている。

実機幅（390×844）で計測したところ、**現状のモバイルは「見えるが操作できない」状態**だった。

| 実測項目 | 値 |
|---|---|
| 表の1行目が始まる位置 | y = **390px**（画面 844px の **46%** がツールバー＋実行中バナー） |
| 行の高さ | **20px** |
| 表の実幅 / 可視幅 | **838px / 356px**（**57% が画面外**） |
| 行の操作ボタン（コピー・編集・削除）の位置 | x = 763〜799 → **すべて画面外** |
| 操作ボタンのサイズ | **18 × 15px** |

つまり **開始 / 終了の打刻ができない**。ライフログの中核操作が物理的に届いていない。
ページ自体の横スクロールは発生していない（`documentScrollWidth` = 390）ので、
壊れているのは「表の中身が入りきらないこと」と「操作要素の配置」である。

本変更の目的は、**スマートフォンで「追加・打刻・今日の確認」ができる状態にすること**。
表ライトの作り込み（列幅最適化・インライン編集・vim 風カーソル移動）は PC のためのものであり、
モバイルへ持ち込まない。

### 1.1 `TaskCards` の復活では足りないこと（検証済み）

`TaskCards.tsx` は 2026-07 に一覧から外されたがコードは残っており、当初はこれを復活させる案だった。
実際に 390px で描画して確認したところ、**モバイル幅では成立しない**。

- **良い点**: 開始 / 終了 / 中断が画面内に収まり、タップできる。表より明確に良い
- **成立しない点**:
  - **タスク名が縦に潰れる**。「洗濯物をたたむ」が3行に折り返す。操作ボタン群が横幅を占め、タスク名の領域が残らない
  - **カード1枚が巨大**。進行中タスク1件で約 500px（画面の6割）。数件見るのに延々スクロールが必要
  - コピー・削除・📎 がカード上部の一等地を占める。ライフログでの使用頻度は低い
  - ツールバーが 46% を占める問題は手つかず

`TaskCards` はデスクトップの広い幅を前提にした設計であり、**モバイル専用の一覧を新規に作る**。
ただし `TaskActions` / `rowStyle` / `sort.ts` / `logic.ts` などの部品はそのまま流用する。

## 2. 潰す対象と、あえて対象外にするもの

| シナリオ | 今回の扱い | 対応 |
|---|---|---|
| スマートフォンから開始 / 終了の打刻ができない | **対象** | モバイル一覧（§4.2） |
| ツールバーが画面の 46% を占める | **対象** | モバイルでの折りたたみ（§4.3）・実行中バナーの圧縮（§4.4） |
| タップ対象が小さすぎる（行 20px・ボタン 18×15px） | **対象** | モバイル一覧で 44px 以上を確保（§4.2） |
| PC とスマートフォンで表示を強制的に切り替えたい | **対象** | URL クエリ `?ui=`（§4.5） |
| PC の表ライトの使い勝手 | **対象外** | 一切変更しない。モバイル幅のときだけ別のビューを出す |
| キーボードショートカット | **対象外** | 変更しない。モバイルでは使われないだけで、害はない |
| 一括編集・連続開始時刻・カレンダー登録・クリップボード取込 | **対象外**（モバイルでは出さない） | 選択操作やPC前提の機能のため、モバイルのツールバーからは省く（§4.3） |
| オフライン動作・PWA 化（ホーム画面へ追加） | **対象外**（将来） | 別テーマ。本変更は表示のみを扱う |
| 画面を消しても終了予定通知を鳴らす | **対象外**（将来） | 前仕様書の付録のとおり、ネイティブ化が必要な別テーマ |

## 3. 決定事項（設計方針）

1. **PC の挙動は一切変えない。** モバイル幅のときだけ別のビューとツールバーを出す。
   広い画面での表示・操作・キーボードショートカットは現状のまま。
2. **表示の切り替えは画面幅で自動**とする（M-1）。設定を増やさない。
   ただし **URL クエリ `?ui=mobile|desktop` で強制できる**ようにする（§4.5）。
3. **モバイルのツールバーは、絞り込み類を既定で折りたたむ**（M-2）。
   常時見えるのは「追加・仕事/個人モード・絞り込みの開閉・データメニュー」だけにする。
4. **モバイル一覧の1行は「タスク名＋状態＋時刻/見積＋打刻ボタン」**（M-3）。
   区分・重要度・繰返・カテゴリは出さない。行をタップすれば既存の詳細編集で見られる。
5. **行に置く操作は打刻だけ**（M-4）。状態に応じて「▶ 開始」「■ 終了」が切り替わる。
   中断・延期・コピー・削除は詳細編集から行う。
6. **並び順・絞り込み・状態判定・繰り返し生成は既存ロジックをそのまま使う。**
   `sort.ts` / `logic.ts` / `visibleTasks` の計算に手を入れない。表とモバイルで同じ結果になること。
7. **強制指定は、それと分かるように表示し、自動へ戻せるようにする。**
   直近の不具合（使えない保存先を無効化して閉じ込めた件）と同じ轍を踏まないため、
   「戻せない状態」を作らない（§4.5）。

## 4. 機能要件（詳細仕様）

### 4.1 モバイル判定と切り替え

- **しきい値は 640px 未満**をモバイルとする（Tailwind の `sm` に合わせる）。
- 判定は `window.matchMedia("(max-width: 639px)")` を購読し、**回転・リサイズに追従**する。
- 判定結果は表示にのみ影響する。データ・絞り込み・並び順・保存には一切影響しない。
- `matchMedia` が使えない環境ではデスクトップ表示にフォールバックする。

### 4.2 モバイル一覧（新規 `TaskListMobile`）

対象は表と同じ `visibleTasks`（絞り込み・並び順は共通）。

**日付の区切り**

- 日付が変わるところに **区切り見出し**を出す（例: `8/30(日)`）。
  表ライトの「日付が変わる行の下に実線」と同じ狙いで、モバイルでは見出しの方が読みやすい。
- 見出しはその日付の合計（見積 / 実績）を右に小さく添える。

**1行の構成**

```
┌──────────────────────────────────────────┐
│ [状態] タスク名 🗺️ 🔗 📝            ［打刻］│   1行目: 状態バッジ ＋ タスク名 ＋ リンク・メモ
│ 07:32〜07:36  実績4m / 見積5m              │   2行目: 時刻と時間（状態で内容が変わる）
└──────────────────────────────────────────┘
```

- **状態バッジ**: 未着手 / 進行中 / 待ち / 完了。既存の `rowStyle.ts` の色を流用する。
- **タスク名**: 1行で表示し、溢れは省略記号。**折り返さない**（`TaskCards` が潰れた原因）。
  メモがあるタスクは既存どおり 📝 を付け、タップでメモダイアログを開く。
- **2行目の内容**（ライフログとして「いつ・どれだけ」が分かることを優先）:
  - 未着手で開始予定あり … `予定 07:30`＋`見積 5m`
  - 未着手で開始予定なし … `見積 5m`（見積 0 のときは省略）
  - 進行中 … `07:32〜`＋`見積 5m`
  - 完了 … `07:32〜07:36`＋`実績 4m / 見積 5m`
- **期限**: 期限切れ・期限当日は既存の警告色（赤・黄）を行の背景に反映する。
  日付テキストは出さない（区切り見出しと重複するため）。色だけで気づければよい。
- **リンク（Issue #88 で追加）**: リンクを持つタスクは、**タスク名の右に絵文字**を並べる。
  - **行は増やさない。** 1行1タスクが崩れると一覧を見渡せなくなる。行の下にチップを敷く案は
    実際に描いて却下した（リンクのあるタスクだけ縦に伸び、一覧が読めなくなる）。
    幅が足りないときに削るのは**タスク名の方**（元から「溢れは省略、全文は詳細編集で」の方針）。
  - 出す絵文字と押したときの動き:
    - **Google マップ … 🗺️**。移動の記録（#86「ここにいる」）が主用途なので、
      🔗 と区別が付くことを要件とする。判定はホスト名で行い、
      `www.google.com/maps/...` `maps.google.co.jp/...` `maps.app.goo.gl/...` `goo.gl/maps/...` を拾う。
      文字列に "maps" を含むだけの他サイトは拾わない。
    - その他の Web リンク … **🔗**。新規タブで開く。
    - ローカルパス（#45） … **📄 / 📁**。ブラウザから開けないので、
      PC と同じくタップでパスをクリップボードへコピーする。
  - 並びは **タスク名 → リンク → 📝（メモ）**。PC（表）と同じ順にする。
  - タップ判定は 28 × 32px を確保し、**タップは行に伝播させない**
    （リンクを押したつもりで詳細編集が開かないようにする）。
- **行の高さ**: 打刻ボタンを含めて **56px 以上**を確保する。
- **行のタップ**: 既存の詳細編集ダイアログを開く（省いた情報はここで見る・直す）。
  打刻ボタンのタップは行のタップに伝播させない。
- **選択（チェックボックス）は出さない。** 一括編集・連続開始時刻をモバイルで使わないため。

**打刻ボタン**

- 未着手 → **「▶ 開始」**、進行中 → **「■ 終了」**、完了 → ボタンを出さない。
- 待ちフラグが立っていても未着手なら「▶ 開始」を出す（表と同じく待ちは並び順にのみ効く）。
- 押したときの動きは PC と完全に同じ。既存の `handleStart` / `handleEnd` をそのまま呼び、
  **時刻入力ダイアログ（`TimeInputDialog`）を経由する**。
  4桁数字入力はスマートフォンのテンキーと相性が良く、そのまま活きる。
- ボタンの当たり判定は **44 × 44px 以上**。
- 読み取り専用の窓（#57）では無効化する。

**空のとき**

- 「表示するタスクがありません。＋ で追加できます。」と出す（既存のカード版の文言をモバイル向けに直す）。

### 4.3 モバイルのツールバー

**常時表示（1段に収める）**

- タイトル（`worklist3`）と ? ボタン
- **＋ 追加**
- 仕事 / 個人 / すべて のモード切替
- **🔍 絞り込みの開閉**（トグル。開いているときは色を変える）
- 💾 データメニュー

**折りたたみの中（🔍 で開く。既定は閉じる）**

- 期間（今日以降 / その他）
- 完了（すべて / 完了のみ / 完了を隠す）
- 予定のみ
- カテゴリ
- タスク名で絞り込み

**モバイルでは出さない**

- クリップボード取込・一括登録・ランダム開始・連続開始時刻・一括編集・カレンダー登録・全選択・選択削除
- 理由: 選択操作やキーボード、PC 上のアプリ（Teams / Outlook）を前提にしており、モバイルでは使えないか使わない。
  データメニュー（💾）の中身は変えない（エクスポート・インポート・バックアップ設定はモバイルでも要る）。

**合計（見積 / 実績 / 残り）**

- 1行に収まらないため、モバイルでは絞り込みの折りたたみの中へ移す。

### 4.4 実行中バナーのモバイル表示

現状、モバイルでは3要素が折り返して約 90px を占めている。

- モバイルでは **1行**に収める。タスク名（省略あり）＋ 残り時間 ＋ 「■ 終了」。
- 「🔔 終了時刻の通知を有効化」はアイコンのみのボタンにする（未許可のときだけ出す）。
- 終了予定を過ぎたときに赤くする挙動、タブタイトルへの反映、通知の発火は**変更しない**。

### 4.5 URL クエリによる強制指定

- **`?ui=mobile`** … 画面幅によらずモバイル表示
- **`?ui=desktop`** … 画面幅によらずデスクトップ表示
- **無指定 / `?ui=auto`** … 画面幅で自動（既定）

実装上の注意:

- `readUrlSettings()` に `ui` を追加する。不正な値は無視（＝自動）。
- **`writeUrlSettings()` は、強制されているときだけ `ui` を書く。**
  自動のときに書いてしまうと、スマートフォンで開いた URL をそのまま PC で開いたときに
  モバイルが強制されてしまう。既定値を書かずに URL を短く保つ既存方針とも一致する。
- **強制中はその旨を小さく表示し、「自動に戻す」を用意する。**
  URL を手で編集しないと戻れない状態を作らない（§3-7）。
  表示場所はツールバーの下端（モバイル）／データメニュー内（デスクトップ）とし、常時目立たせない。

## 5. 実装方針（技術メモ）

- `src/lib/useIsMobile.ts` … `matchMedia` の購読フック。強制指定を引数で受け、最終的な判定を返す。
  外部依存が薄いので、判定関数（幅と強制指定から表示を決める純粋関数）だけ切り出してテスト対象にする。
- `src/components/TaskListMobile.tsx` … 新規。表とカードのどちらの派生でもない、モバイル専用の一覧。
  `rowStyle.ts` の色、`logic.ts` の状態判定、`date.ts` の整形を流用する。
- `src/components/Toolbar.tsx` … `compact`（モバイル）で表示を出し分ける。**既存の並びとロジックは変えず、
  括り出しと表示条件の追加にとどめる。** ここを作り直すと PC 側の退行が出るため。
- `src/components/RunningBanner.tsx` … `compact` を追加して1行にする。
- `src/lib/urlParams.ts` … `ui` の読み書き（§4.5）。
- `src/App.tsx` … 判定に応じて `TaskTable` と `TaskListMobile` を出し分ける。
  **`visibleTasks` の計算・並び順・ハンドラは共有し、分岐は描画だけにする。**
- `TaskCards.tsx` の扱いは §9 の判断待ち。

## 6. UI変更点

- **モバイル（640px 未満）**: ツールバーが1段＋折りたたみになり、一覧がモバイル一覧に変わる。
  実行中バナーが1行になる。
- **デスクトップ（640px 以上）**: **変更なし。**
- 強制指定中のみ、「モバイル表示を強制中（自動に戻す）」の小さな表示が出る。

## 7. ファイル別・変更概要

| ファイル | 変更 |
|---|---|
| `src/components/TaskListMobile.tsx` | 新規（日付見出し・1行の構成・打刻ボタン）。#88 でタスク名の右にリンクの絵文字を追加 |
| `src/lib/link.ts` | #88: `isMapsUrl`（Google マップ判定）と `linkIcon`（表示する絵文字の決定）を追加 |
| `src/lib/useIsMobile.ts` | 新規（`matchMedia` 購読と、幅＋強制指定から表示を決める純粋関数） |
| `src/lib/urlParams.ts` | `ui` の読み書き。自動のときは書かない |
| `src/components/Toolbar.tsx` | モバイル時の折りたたみと、出さない操作の出し分け |
| `src/components/RunningBanner.tsx` | モバイル時に1行へ圧縮 |
| `src/App.tsx` | 表とモバイル一覧の出し分け、強制指定の受け渡し |
| `README.md` / 本仕様書 | 記述の更新 |

## 8. 非機能・エッジケース

- **横向き（ランドスケープ）**: 幅で判定するため、横向きにすると 640px を超えてデスクトップ表示になりうる。
  これは意図した挙動とする（横向きなら表も読める）。`?ui=mobile` で固定できる。
- **タブレット**: 640px 以上なのでデスクトップ表示。表を横スクロールして使う。強制指定で変えられる。
- **回転・ウィンドウリサイズ**: `matchMedia` の購読で追従する。切り替わってもスクロール位置とデータは保つ。
- **読み取り専用の窓（#57）**: モバイルでも打刻ボタンを無効化する。
- **実行中タスク**: 絞り込みで隠れていてもバナーには出す（既存仕様どおり）。
- **長いタスク名**: 1行で省略する。全文は行タップ（詳細編集）で見る。
  デスクトップのホバー表示はモバイルに無いため、省略記号だけに頼らない導線が要る。
- **リンクが多いタスク（#88）**: 絵文字が増えたぶんタスク名が短く切り詰められる。
  これは意図した優先順位（リンクは押せることが要、タスク名は詳細編集で全文が読める）。
  リンク欄は詳細編集で最大5件までなので、絵文字が無限に増えることはない。
- **ローカルパスをモバイルで押したとき（#88）**: コピーは成功するが、貼り付け先（エクスプローラ）は
  スマートフォンに無い。承知のうえで PC と同じ挙動にしておく（PC に戻ってから貼れる）。
- **パフォーマンス**: 件数が増えても表と同じ `visibleTasks` を描くだけなので、現状と同等。

## 9. 要検討事項（実装前に判断したい点）

- **`TaskCards.tsx` をどうするか。** 現状は未使用で、モバイル幅では成立しないことを確認済み。
  モバイル一覧が入れば役割は完全に置き換わる。**削除を提案する**（git 履歴には残る）が、
  `types.ts` の `LayoutMode` に `"cards"` を残す方針と合わせて判断したい。
- 折りたたみを開いた状態を記憶するか（`localStorage`）。既定は「毎回閉じる」で足りるか。
- 日付の区切り見出しに合計（見積 / 実績）を出すかどうか。出すと情報は増えるが行数も増える。

## 10. 完了条件（受け入れ基準）

**実ブラウザ（Chromium）での確認を必須とする。** テストが通ることだけを根拠にしない。

モバイル幅（390 × 844）で:

- **タスクの1行目が y ≤ 200px から始まる**（現状 390px。ツールバー＋バナーが画面の 1/4 以下）
- **ページも一覧も横スクロールしない**（`documentScrollWidth` ≤ ビューポート幅）
- **打刻ボタンが可視域内にあり、当たり判定が 44 × 44px 以上**
- 未着手で「▶ 開始」、進行中で「■ 終了」が出て、押すと時刻入力ダイアログが開き、PC と同じ結果になる
- 行をタップすると詳細編集が開く。打刻ボタンのタップでは開かない
- 日付が変わるところに区切り見出しが出る
- 絞り込みは既定で閉じており、🔍 で開閉できる
- 実行中バナーが1行に収まる
- 一覧の並び順と絞り込みの結果が、同じデータのデスクトップ表示と一致する

デスクトップ幅（1280 × 900）で:

- **表示・操作ともに現状から変化がない**（退行なし）
- キーボードショートカットが従来どおり動く

URL 強制指定:

- `?ui=mobile` で、広い画面でもモバイル表示になる
- `?ui=desktop` で、狭い画面でもデスクトップ表示になる
- 強制中はその旨が表示され、「自動に戻す」で解除できる
- 自動のときは URL に `ui` が書かれない（スマートフォンで作った URL を PC で開いても強制されない）

共通:

- `npm test` / `npm run build` が通る
- 幅と強制指定から表示を決める純粋関数の単体テストがある
- `pageerror` が出ない

---

## 付録：今回スコープ外とした案

**現時点では実装しない。**

- **PWA 化（ホーム画面へ追加・`storage.persist()`）**: モバイルの localStorage は退去されうるため、
  本来は必要。ただし表示の改善とは別テーマなので分ける。
- **画面を消しても終了予定通知を鳴らす**: 現状の通知は前面タブの1秒間隔タイマー発火のため、
  Android では画面を消すと止まる。確実に鳴らすにはネイティブ化（Capacitor）が要る。
- **スワイプ操作（左スワイプで完了など）**: 打刻ボタンで足りるかを使って確かめてから検討する。
- **下部固定バー**: 親指で届く利点はあるが、実行中バナーと場所を取り合う。
  まず折りたたみで高さが足りるかを確かめる。
