# worklist3 変更仕様書：バックアップ先の追加（Googleドライブ）と圧縮

> 本書は Claude Code に実装を依頼するための変更仕様書です。
> 既存の `変更仕様書_自動バックアップ.md`（以下「前仕様書」）の続編にあたり、その決定事項を原則そのまま引き継ぎます。
> スマートフォン（Android）からの利用を見据え、**File System Access API が使えない環境でも自動バックアップを成立させる**ことが動機です。

## 1. 目的

現在の自動バックアップは File System Access API（以下 FSA）でローカルの同期フォルダへ書き出す方式で、
`アプリ → ローカルFS →（OneDrive等の同期クライアント）→ クラウド` という構図で成り立っている。

**Android にはこの構図が存在しない。** Android Chrome に FSA が無く、さらに Android のクラウド同期アプリ
（Google ドライブ / OneDrive / Dropbox）は**任意のローカルフォルダをミラーする機能を持たない**ため、
「ローカルに書いてクラウドに任せる」が原理的に使えない。

そこで、**バックアップ先として Google ドライブ（API 経由）を選べるようにする**。
あわせて、モバイル回線での通信量を実用域に収めるため**保管形式の gzip 圧縮**と、それを読み戻すための
**インポートの gz 対応**を行う。

## 2. 潰す対象と、あえて対象外にするもの

| シナリオ | 今回の扱い | 対応 |
|---|---|---|
| FSA 非対応環境（Android 等）でバックアップが取れない | **対象** | Google ドライブ保存先の追加（§4.3） |
| モバイル回線での通信量・電池消費 | **対象** | 圧縮（§4.5）＋書き込み契機の見直し（§4.3.4）＋日次コピーのサーバー側複製（§4.3.5） |
| Drive 上の控えからの復元導線（エクスプローラが使えない） | **対象** | Drive バックアップ一覧からの復元（§4.7） |
| 圏外・オフライン時の書き込み | **対象** | 未送信状態の表示と復帰時フラッシュ（§4.8） |
| 複数端末が同じ Drive ファイルを潰し合う | **対象** | 端末名によるファイル名の分離（§4.3.2） |
| **複数端末の同期（データの一本化）** | **対象外**（別テーマ） | 本変更は一方向バックアップのみ。マージ・tombstone は扱わない（付録参照） |
| **同時に複数の保存先へ書く（多重化）** | **対象外** | 複雑さに見合わないため見送り。保存先は常に1つ |
| localStorage の容量上限・アーカイブ | **対象外**（将来） | 前仕様書 付録のとおり、容量が課題化した段階で対応 |

## 3. 決定事項（設計方針）

1. **前仕様書の設計契約を維持する。** localStorage は主保存のまま。バックアップは付加機能であり、
   **未接続・権限切れ・書き込み失敗・圏外でもアプリ本体は動き続ける**。
2. **保存先は「ローカルフォルダ」「Googleドライブ」の排他選択**とする。同時に両方へは書かない。
   どちらを使うかは端末ごとにユーザーが選ぶ（例：会社PCはローカルフォルダ、スマートフォンはDrive）。
3. **`BackupTarget` インターフェースを導入し、方針ロジックと書き込み手段を分離する。**
   デバウンス・サニティガード・ローテーション・状態通知・スヌーズは**保存先によらず共通**とし、
   「どこへどう書くか」だけを差し替える。既存の振る舞いは1バイトも変えない。
4. **Drive のスコープは `drive.file` を使う。** アプリが作成したファイルのみにアクセスでき、かつ
   **ユーザーが Drive の画面で通常どおり閲覧・ダウンロードできる**。`drive.appdata`（隠し領域）は
   最後の砦を目視できなくなるため採用しない。制限付きスコープではないため重い審査も不要。
5. **圧縮はブラウザ標準の `CompressionStream("gzip")` を使う。** 依存ライブラリは追加しない。
   非対応環境では圧縮オプションを提示しない（FSA と同じ `supported` パターン）。
6. **復元は引き続きインポート機能を経由する**（前仕様書 §3-6）。ただし Drive はエクスプローラで開けないため、
   **「Drive上の控えを一覧して選ぶ」導線のみ追加**し、選んだ後は既存のインポートフローへ合流させる。
   新しいマージ規則やデータ書き換え経路は作らない。
7. **本変更は一方向バックアップである。** 復元以外にクラウドからデータを取り込む経路は作らない。

### 3.1 個別の判断（確定済み）

| 項目 | 決定 | 理由 |
|---|---|---|
| 複数端末による同一ファイルの上書き | **接続時に「端末名」を入力させ、`worklist3-<端末名>.json` とする** | 設定項目を増やさず衝突を防げる。同じ端末名を再入力すれば同じファイルに復帰でき、再接続・復元とも整合する |
| 保存先切替時のサニティガード誤発火 | **現行の挙動を踏襲**（保留して警告し、「⤓ 今すぐバックアップ」で明示的に上書き） | 追加実装ゼロ。逃げ道が既にある。接続時に既存ファイルの件数を比較元に読む設計（`attach()`）は Drive にも必ず実装する |
| 圧縮切替で新旧2つのミラーが残る | **切替時に旧形式のミラーをゴミ箱へ移す** | ミラーを常に1つに保つ。ゴミ箱は猶予があるため取り返しがつく |
| Drive からの復元導線 | **Drive上の控えを一覧して選ぶUIを追加**（前仕様書 §3-6 を意識的に拡張） | ローテーション掃除のために `files.list` は結局実装するので追加コストが小さい。緊急時に「一昨日の状態」へ最短で戻れる |
| オフライン時の扱い | **専用の状態表示にとどめ、⚠ 警告は出さない。復帰時に自動フラッシュ** | 移動のたびに警告が出ると実用に耐えない。`pending` は最新スナップショット1件のみ保持する設計なので、復帰時に最新を書けば足りる |
| Drive のファイル配置 | **アプリが `worklist3` フォルダをマイドライブ直下に作成する** | 設定不要。Picker API とその API key を持ち込まずに済む。ユーザーからは普通に見える |

## 4. 機能要件（詳細仕様）

### 4.1 保存先の抽象化

- `BackupTarget` インターフェースを定義し、`backup.ts` の**方針ロジックは一切変更せず**、書き込み手段のみを委譲する。

```ts
export interface BackupTarget {
  readonly id: "fsa" | "gdrive";
  readonly label: string;              // 「ローカルフォルダ」「Google ドライブ」
  readonly supported: boolean;         // 環境が対応しているか
  readonly displayName: string;        // 接続先の表示名(フォルダ名 / 端末名)
  connect(): Promise<boolean>;         // ユーザー操作起点。フォルダ選択 / OAuth
  restore(): Promise<boolean>;         // 起動時。保存済み接続情報から復帰(非対話)
  reconnect(): Promise<boolean>;       // 権限・トークン切れからの再接続(対話)
  disconnect(): Promise<void>;
  readMirrorCount(): Promise<number | null>;   // サニティガードの比較元
  writeMirror(body: BackupBody): Promise<void>;
  writeDaily(body: BackupBody, date: string): Promise<void>;
  listDaily(): Promise<DailyEntry[]>;          // 掃除と復元一覧の両方で使う
  deleteDaily(entry: DailyEntry): Promise<void>;
  readFile(entry: DailyEntry | "mirror"): Promise<string>;  // 復元用。展開済みJSON文字列を返す
}
```

- 保存先ごとに**独立して**保持する状態：接続情報、`lastBackedUpCount`、デバウンス間隔、圧縮既定値。
  - **現行の `lastBackedUpCount` はモジュールレベルの単一変数（`backup.ts:178`）。保存先ごとに持たせる。**
    そうしないと保存先を切り替えたときにサニティガードの比較元がずれる。
- 既存の `BackupState` に保存先を識別する情報（`targetId` / `targetLabel`）と、オフライン状態（§4.8）を追加する。

### 4.2 ローカルフォルダ保存先（既存踏襲）

- 現行 `backup.ts` の FSA 実装を `FsaTarget` として切り出す。**書き出されるファイル・タイミング・警告は現行と同一**であること。
- 既存ユーザーの移行：IndexedDB に保存済みのディレクトリハンドルを、**起動時に黙って「ローカルフォルダ保存先が接続済み」として引き継ぐ**。
  ユーザーから見て何も変わらないこと。設定のやり直しを求めない。
- 既定の保管形式は**非圧縮**（ディスクは安く、人が読める方が価値が高い）。圧縮も選択可（§4.5）。

### 4.3 Googleドライブ保存先

#### 4.3.1 認証

- `gcalClient.ts` の方式を踏襲する。GIS（Google Identity Services）でアクセストークンを取得し、
  **トークンはメモリのみに保持し永続化しない**。localStorage に置くのは Client ID など機微でない値だけ。
- スコープは `https://www.googleapis.com/auth/drive.file`。
- Client ID は 💾 メニューでユーザーが設定する（カレンダー連携と同じ方式。ソースへ埋め込まない）。
  カレンダー連携と同一の Client ID を使い回してよい。
- **承認済み JavaScript 生成元**：`https://gokw.github.io`（GitHub Pages 本番）と `http://localhost:5173`（開発）。
- トークンは約1時間で失効する。失効後の最初の書き込み時に **`prompt: ""` によるサイレント再取得を1回試みる**。
  成功すれば何事もなく続行、失敗したら `needsReconnect` を立てて「🔄 再接続」を促す（FSA の権限切れと同じ導線）。
- **現行の設計思想を守る**：ユーザー操作起点でない場面で認証ポップアップを出さない（`ensurePermission(dir, false)` と同じ扱い）。

#### 4.3.2 ファイル配置と命名

- 初回接続時に **「この端末の名前」を入力させる**（例：`スマホ`、`自宅PC`）。空文字は許可しない。
- マイドライブ直下に `worklist3` フォルダを作成し、その中に置く：
  - ミラー：`worklist3-<端末名>.json`（圧縮時は `worklist3-<端末名>.json.gz`）
  - 日次コピー：`worklist3/backups/worklist3-<端末名>-YYYY-MM-DD.json`（圧縮時は `.json.gz`）
- 端末名は接続情報として保存し、再接続時は再入力を求めない。ユーザーが明示的に変更する手段は 💾 メニューに置く。
- **同じ端末名で接続し直せば、同じファイルを引き継ぐ**（ブラウザデータを消した後の復帰経路になる）。
- 作成した `fileId` は保存して毎回の検索を省く。**ファイルが手動削除されていた場合は 404 で作り直す**
  （カレンダー連携の `gcalEventId` と同じフォールバック方式）。

#### 4.3.3 書き込み

- 単一リクエストの multipart アップロードで**全体を1回で置き換える**（サイズが小さいため resumable upload は不要）。
  読み手からは新リビジョンが一斉に見えるため、FSA の `createWritable()` と同等の原子性が得られる。
- 書き込み失敗時は前仕様書と同じく `reportProblem()` に落とし、**アプリ本体は止めない**。

#### 4.3.4 書き込み契機（デバウンス）

- Drive 保存先のデバウンスは **30秒**とする（FSA は現行どおり 1.5秒）。`DEBOUNCE_MS` を保存先の属性へ移す。
- あわせて **`visibilitychange` で `hidden` になった時点でも即時フラッシュ**する。
  - スマートフォンでは「アプリを離れて端末を置く」が典型的な離脱であり、ここで書ければ実質的な損失窓はほぼ消える。
  - ただし**ページ破棄中の `fetch` は完了が保証されない**。`sendBeacon` は認証ヘッダとアップロード形式の都合で使えないため、
    **「大抵間に合うが保証はない」と割り切り**、定期フラッシュを併用する。

#### 4.3.5 日次コピーとローテーション

- **現行実装は書き込みのたびにミラーと日次コピーの両方を書いている**（`writeBackup()`）。
  ローカルでは正しい（常に最新の日次控えが残る）が、Drive ではそのまま流用すると通信量が倍になる。
- Drive では**日次コピーを `files.copy` によるサーバー側複製で作る**（アップロードのバイト数がゼロになる）。
  ミラーを書いた後に同日ファイルへ複製する形とし、その日の2回目以降は既存の日次ファイルを複製で上書きする。
  - **`files.copy` が `drive.file` スコープで期待どおり動くことは、実装時に実地で確認すること**（§9）。
    動かない場合は「その日の最初の1回だけ本体をアップロードする」方式へフォールバックする。
- 保持世代は現行どおり **14日**。超過分は**完全削除ではなくゴミ箱へ移す**（`trashed: true`）。
  掃除の誤りが起きても猶予期間内に取り返せるようにする。
- **掃除の対象判定を拡張子込みで行うこと。** 現行の正規表現は
  `/^worklist3-(\d{4}-\d{2}-\d{2})\.json$/` であり、**`.json.gz` にも端末名付きの名前にも一致しない**。
  一致しないと古い世代が消えず無限に増えるため、必ず更新する。
  README に記載の「`worklist3-日付.json` 以外のファイルには触りません」という約束も、
  **「`worklist3-<端末名>-日付.json`（`.gz` 含む）以外には触らない」**へ書き換える。

### 4.4 保存先の切替

- 💾 メニューで保存先を選ぶ。切替時は現在の保存先を `disconnect()` してから新しい保存先に `connect()` する。
- **切替前の保存先のファイルは一切削除しない。** 併用期間の保険として残す。
- 接続直後に `readMirrorCount()` で既存ファイルの件数を読み、サニティガードの比較元とする（§3.1 の決定）。
  - 比較元が取れない（ファイルが無い）場合は現行どおり比較しない。
  - 件数差でガードが発火した場合は保留して警告し、ユーザーが「⤓ 今すぐバックアップ」で明示的に上書きできる。
- 新しい保存先の日次ローテーションはゼロから始まるため、**14日分が貯まるまで世代が薄い**。
  切替直後は 💾 メニューにその旨を表示し、旧保存先のファイルを消さないよう促す。

### 4.5 圧縮（バックアップ・エクスポート）

- 保管形式として **gzip 圧縮を選択できる**ようにする。対象は「自動バックアップ」と「エクスポート」の2箇所。
- 既定値：**ローカルフォルダ保存先 = 非圧縮 / Google ドライブ保存先 = 圧縮 / エクスポート = 非圧縮**。いずれも変更可。
- 圧縮時はシリアライズを**整形なし**（`JSON.stringify(tasks)`）にする。
  非圧縮時は現行どおり整形あり（`JSON.stringify(tasks, null, 2)`）で人が読める形を保つ。
- **書き込み前に検証する。** 圧縮した結果をその場で展開し、`JSON.parse` が通り件数が一致することを確かめてから書く。
  壊れた控えを持っていることに気づかない状態が最悪であり、サニティガードと同じ思想でここは投資する。
  検証に失敗した場合は書き込まず `reportProblem()` に落とす。
- 圧縮設定を変更したときは、**旧形式のミラーをゴミ箱へ移す**（ローカルフォルダの場合は削除）。
  日次コピーの旧形式は保持期間の経過とともに自然に消えるため触らない。
- `CompressionStream` 非対応環境では圧縮の選択肢を表示しない。

#### 参考：実測値（合成データ・1000件）

| 形式 | サイズ |
|---|---|
| 整形あり（現行 `serializeTasks`） | 498KB |
| 整形なし | 366KB |
| 整形なし + gzip | **25KB** |

圧縮比は約 20倍。同形のレコードが並ぶ JSON のため、件数が増えても比率はほぼ変わらない（2000件でも約20倍）。

### 4.6 インポートの gz 対応

- 既存のインポート（追加読込／全リセット読込の選択を含む）で、**gzip 圧縮されたファイルもそのまま読める**ようにする。
- **判別は拡張子ではなく先頭2バイトのマジックナンバー（`1F 8B`）で行う。** 拡張子が変わっていても読める。
- 展開は `DecompressionStream("gzip")`。展開後は既存の経路（`isValid` → `migrateTask` → マージ）に合流し、
  **マージ規則・結果ダイアログの表示は一切変更しない**。
- エラーメッセージは「展開に失敗」と「JSONとして読めない」を区別する。

### 4.7 Driveのバックアップ一覧からの復元

- 💾 メニューに「Drive の控えから復元」を追加する。Drive 保存先に接続しているときだけ表示する。
- ミラー1件と日次コピー（最大14件）を**日付の新しい順に一覧表示**する。各行にファイル名・日付・件数・サイズを出す。
- 選択すると内容を取得し、**既存のインポートフローへそのまま流す**（`ImportModeDialog` で追加／全リセットを選び、
  結果ダイアログで件数と上書き内容を確認する）。新しいデータ書き換え経路は作らない。
- 一覧の取得には `listDaily()` を使う。ローテーション掃除と同じ実装を共有する。

### 4.8 オフライン時の扱い

- `navigator.onLine` が false、またはネットワーク起因の失敗が起きた場合、`BackupState` を
  **「オフライン（未送信）」状態にし、⚠ 警告およびトーストは出さない**。
- `online` イベントで**保留中の最新スナップショットを自動フラッシュ**する。
  現行の `pending` は最新の1件のみを保持する設計のため、これで十分（差分の積み上げは不要）。
- 復帰後の書き込みも失敗した場合は、通常の `problem` として ⚠ を出す。
- ローカルフォルダ保存先はオフラインでも書けるため、この状態を取らない。

### 4.9 サニティガード・ローテーション（既存の踏襲）

- しきい値（0件、または前回比50%以上かつ5件以上の減少）、保持世代（14日）、
  スヌーズ（15/30/60分）、「⚠ バックアップ未実行」の常時表示は**現行のまま変更しない**。
- ガードは保存先によらず共通のロジックとし、比較元だけを保存先ごとに持つ。

## 5. 実装方針（技術メモ）

- `src/lib/backup.ts` … 方針ロジック（デバウンス・直列化・ガード・ローテーション判定・状態通知・スヌーズ）を維持。
  書き込みは `BackupTarget` へ委譲する。**この分離により、フェイクターゲットを差せば
  デバウンス・ガード・ローテーション計算を初めて自動テストできる**（現状テストが無い領域）。
- `src/lib/backupTargets/fsa.ts` … 現行 FSA 実装の切り出し。振る舞いを変えない。
- `src/lib/backupTargets/gdrive.ts` … Drive REST + GIS。**外部依存のため自動テスト対象外**
  （`gcalClient.ts` と同じ位置づけ）。
- `src/lib/gzip.ts` … 圧縮・展開・マジックナンバー判定。**純粋ロジックなのでテスト対象**
  （`gcalMap.ts` と同じ位置づけ）。Node 22 に `CompressionStream` / `DecompressionStream` があるため
  Vitest でラウンドトリップを検証できる。
- 読み取り専用の窓（多重起動ロック #57）は、現行どおり `notifyTasksChanged` を呼ばないため、
  保存先が Drive になっても書き込みは発生しない（`App.tsx:216` の分岐がそのまま効く）。**変更不要だが確認すること。**

## 6. UI変更点（💾 メニュー）

- **保存先の選択**：「ローカルフォルダ」「Google ドライブ」のラジオまたはチップ。非対応の保存先は選べないように表示する。
- **Google ドライブ選択時**：Client ID の設定欄、接続ボタン、端末名の表示と変更、接続状態。
- **保管形式**：「圧縮する（.gz）」のトグル。保存先ごとに記憶する。非対応環境では非表示。
- **エクスポート**：「⬇ エクスポート（JSONを保存）」に圧縮の選択を追加する。
- **復元**：「Drive の控えから復元」（Drive 接続時のみ）。
- **状態表示**：現行の「バックアップ先 / 最終バックアップ時刻 / 保留・エラー / 再接続」に加え、
  オフライン（未送信）と、切替直後の「世代が貯まるまで旧保存先を残してください」の案内。
- 既存の「⚠ バックアップ未実行」「しばらく黙らせる（15/30/60分）」は変更しない。

## 7. ファイル別・変更概要

| ファイル | 変更 |
|---|---|
| `src/lib/backup.ts` | `BackupTarget` への委譲。`lastBackedUpCount` の保存先別化。デバウンス間隔の保存先別化。オフライン状態。`BackupState` の拡張 |
| `src/lib/backupTargets/fsa.ts` | 新規（現行 FSA 実装の切り出し）。ローテーション掃除の正規表現を拡張子・端末名込みへ更新 |
| `src/lib/backupTargets/gdrive.ts` | 新規（GIS 認証・multipart アップロード・`files.list` / `files.copy` / ゴミ箱移動・404 フォールバック） |
| `src/lib/gzip.ts` | 新規（圧縮・展開・マジックナンバー判定・書き込み前の検証） |
| `src/lib/storage.ts` | `serializeTasks` に整形の有無を選ぶ引数。エクスポートの圧縮対応 |
| `src/App.tsx` | インポートの gz 対応（展開してから既存経路へ）。復元一覧ダイアログの接続 |
| `src/components/Toolbar.tsx` | 💾 メニューの UI（§6） |
| `src/components/RestoreFromDriveDialog.tsx` | 新規（控えの一覧と選択） |
| `README.md` / 本仕様書 | 「触らないファイル」の約束の更新を含む記述の更新 |

## 8. 非機能・エッジケース

- **通信量の想定**：800件（現行約400KB）・圧縮あり・1日8回書き込みで **月あたり約5MB**。日次コピーは
  サーバー側複製のため加算されない。圧縮なしのまま 1.5秒デバウンスで Drive へ書くと月300MB規模になるため、
  §4.3.4 と §4.5 は両方とも必須。
- **電池**：通信量とは別問題であり、無線を起こす回数で決まる。デバウンス延長と離脱時フラッシュで対処する（§4.3.4）。
- **Drive 側で手動削除された場合**：404 を検知してファイルを作り直す。ユーザーには通知するが失敗扱いにしない。
- **Drive 側にファイルが既に存在する場合**：件数を読んでガードの比較元とする（§4.4）。上書きの可否はガードに委ねる。
- **端末名の重複**：ユーザーが2台に同じ端末名を付けた場合は同じファイルを共有し、後勝ちで上書きされる。
  仕様上これは防げないため、💾 メニューに「端末ごとに違う名前を付けてください」と明記する。
- **時刻・タイムゾーン**：日次ファイル名はローカル日付 `YYYY-MM-DD`（現行と整合）。
- **クォータ**：`drive.file` の通常利用で問題になる水準ではないが、429/5xx は指数バックオフで再試行し、
  最終的に失敗したら `reportProblem()` に落とす。

## 9. 要検討事項（実装時に確認する点）

- **`files.copy` が `drive.file` スコープで動作するか**を実地で確認する。動かない場合は §4.3.5 のフォールバックへ。
- サイレント再取得（`prompt: ""`）の実効性。ポップアップブロックとの相互作用を実機で確認する。
- Drive 保存先でのデバウンス 30秒が、体感（「変更したのにまだ控えが古い」）として許容できるか。
- 復元一覧に表示する「件数」を得るには各ファイルの取得が必要になる。一覧表示時に全件取得すると重いため、
  メタデータ（サイズ・更新日時）のみを既定とし、件数は選択時に表示する形でよいか。

## 10. 完了条件（受け入れ基準）

- 💾 メニューから保存先を「ローカルフォルダ」「Google ドライブ」で切り替えられる。
- **既存ユーザーが起動したとき、ローカルフォルダの設定が引き継がれ、書き出されるファイルと挙動が従来と同一である。**
- Android Chrome（FSA 非対応）で Google ドライブを保存先に選び、自動バックアップが動作する。
- Drive 上に `worklist3/worklist3-<端末名>.json(.gz)` と `worklist3/backups/worklist3-<端末名>-YYYY-MM-DD.json(.gz)` が作られる。
- 保持期間を過ぎた日次コピーがゴミ箱へ移動し、**`.gz` 形式でも掃除が効く**。
- 圧縮ありで書いた控えを、圧縮設定に関係なくインポートで読み戻せる（マジックナンバー判定）。
- 圧縮設定を切り替えると、旧形式のミラーが残らない。
- エクスポートで圧縮の有無を選べ、どちらもインポートで読み戻せる。
- 「Drive の控えから復元」で一覧が表示され、選んだ控えが既存のインポートフローで取り込める。
- オフラインにしても ⚠ 警告が出ず「未送信」と表示され、復帰時に自動で書き込まれる。
- トークン失効後の書き込みで、サイレント再取得に成功すれば継続し、失敗すれば「🔄 再接続」が促される。
- タスクが0件／急減した状態で保存先を切り替えても、既存の控えが自動上書きされない。
- 保存先が未接続・圏外・認証失敗のいずれでも、アプリ本体は従来どおり動作する。
- フェイクターゲットにより、デバウンス・サニティガード・ローテーション判定の単体テストが通る。
- 圧縮・展開・マジックナンバー判定のラウンドトリップ単体テストが通る。

---

## 付録：今回スコープ外とした案

**現時点では実装しない。** 前仕様書 付録の内容は引き続き有効であり、以下はそれを本変更の文脈で更新したもの。

- **複数端末でのデータ同期（案A）**：PC とスマートフォンで同一のデータセットを共有する構想。
  本変更で Drive への読み書き経路ができるため、残る差分は
  **タスク単位マージ（`id` で突合し `updatedAt` の新しい方を採用）＋削除の tombstone 化**のみとなる。
  ただし採用する場合、**スマートフォン側のデータに仕事のタスクが含まれるため、
  会社のデータが個人の Google ドライブへ載る**。会社PCをローカルフォルダ運用にするだけでは防げないため、
  **着手前に取り扱いの可否を再確認すること**。
- **同時に複数の保存先へ書く（多重化）**：ローカルとDriveの両方へ書く案。安全側だが、
  保存先ごとの状態・ガード・失敗処理が二重になり複雑さに見合わないため見送った。
- **Capacitor によるネイティブ化**：本変更により**バックアップのためのネイティブ化は不要**になった。
  残る動機は「実行中タスクの終了予定通知を、画面を消した状態でも確実に鳴らす」ことのみ
  （現行の通知は前面タブの1秒間隔タイマー発火のため、Android では画面を消すと止まる）。別テーマとする。
- **モバイル向け表示（カード形式の復活）**：`TaskCards.tsx` と `LayoutMode` の `"cards"` は残してあるため、
  画面幅による自動切替で復活できる。本変更とは独立の別テーマ。
- **古い完了タスクのアーカイブ**：ライフログとしての利用ではデータが増え続けるため、
  いずれ必要になる。圧縮により当面は猶予ができたが、`localStorage` の容量上限と起動時のパース時間には効かない。
