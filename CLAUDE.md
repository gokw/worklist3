# worklist3 — 作業するときの前提

このファイルは Claude Code が毎回最初に読む。**実際に使っている環境**と、
そこで分かっている制約を書いておく。ここが空だと、毎回 Chrome 前提の
案内をするような回り道が起きる。

## 利用環境

| どこ | 端末 / ブラウザ | 備考 |
|---|---|---|
| 個人スマートフォン | OPPO Reno9 A / **Chrome** | PWA としてインストール済み。**Brave は使わない**(理由は下記) |
| 会社スマートフォン | **Edge** | ローカルフォルダ保存先は選べない(File System Access API が無い。#102 §4-1 で実機確認) |
| PC(会社・自宅) | Chrome / Edge | ローカルフォルダ保存先(File System Access API)が使えるのはここだけ |

## 分かっている制約

- **Brave ではアプリのショートカットが出ない。** PWA としてインストールはできる
  (ホーム画面に本物のアプリとして入る)が、アイコンを長押ししても manifest の
  `shortcuts` が出ず、「ここにいる記録」へ直行できない。Chrome では出る。
  このため個人スマートフォンは Chrome を使う。
- **スマートフォンのブラウザでは「ローカルフォルダ」保存先を選べない。**
  File System Access API がデスクトップの Chromium 系にしか無いため。
  同期フォルダでファイルが端末に届いていても、ブラウザがフォルダを掴めない。
- **データ(localStorage)はブラウザごとに別物。** ブラウザを変えるとタスクは
  引き継がれない。移すならエクスポート/インポートか Drive の控えを経由する。
- **オフラインでは起動できない。** PWA 段階1では Service Worker を入れていない。

## PWA(段階1)の実機確認 — 2026-09-05、OPPO Reno9 A / Chrome

manifest とアイコンだけの段階1(Service Worker なし)を実機で確認し、**成立した**。

- ホーム画面へアプリとしてインストールできる(WebAPK。長押しで「アプリ情報」が出る)
- **アイコンを再タップしても窓が増えない。** `launch_handler` の
  `client_mode: navigate-existing` が効いている。これで #105 の
  「ショートカットで開くたびに新しいタブが開き、前のタブが書き手ロック(#57)を
  持っているせいで読み取り専用になる」現象は解消した
- **standalone の窓でも Drive の接続(GIS のポップアップ方式)が通る。**
  着手前に最大の懸念としていた点で、実機では問題にならなかった
- アイコン長押しから「ここにいる記録」へ直行できる(Chrome のみ。Brave では出ない)

Service Worker(段階2)へ進むかは未定。進める動機は「圏外でも起動して記録したい」
という具体的な要求が出たときだけで、それまでは恒久的なキャッシュ管理の負担と、
全端末(会社PCを含む)へ配られる影響に見合わない。

## 変更の記録はどこにあるか

大きな変更は、根拠と却下案まで含めてリポジトリ直下の Markdown に残してある。
実装に入る前に、関係するものを読むこと。

- `変更仕様書_*.md` … 実装した変更の仕様と決定理由
- `検討_*.md` … 実装するかどうかを決めるための検討(実装しなかったものを含む)
- `README.md` … 利用者向けの説明と、設計の要約

## よく使うコマンド

```
npm test          # vitest。純粋関数の単体テスト
npm run build     # tsc --noEmit を通してから vite build
npm run dev       # 開発サーバ
```

判定・状態遷移のロジックは純粋関数へ切り出して `*.test.ts` の対象にする、
というのがこのリポジトリの方針。UI から呼ぶ前にそこを固める。
