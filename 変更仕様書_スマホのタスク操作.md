# worklist3 変更仕様書：スマホのタスク操作(長押しメニューとスワイプ)

> 本書は Claude Code に実装を依頼するための変更仕様書です。Issue #100 に対応します。
> 既存の `変更仕様書_モバイル対応.md` の決定事項を引き継ぎます(1行1タスク・打刻優先・行タップで詳細編集)。

## 1. 目的

スマートフォンで、日々いちばん多い「日付をずらす」「消す」をその場で片手で終えられるようにする。

現在、モバイル一覧で行からできるのは**打刻(開始/終了)と行タップ(詳細編集)だけ**である。
延期・前日・翌日・削除は、詳細編集ダイアログを開いて日付欄を直すか、PC で操作するしかない。
PC にはこれらのショートカット(P / Ctrl+H / Ctrl+L / Delete)が揃っているのに、
外出先で使うスマホ側にだけ無い状態になっている。

## 2. 潰す対象と、あえて対象外にするもの

| 課題 | 扱い | 方針 |
|---|---|---|
| 日付をずらすのにダイアログを開く必要がある | **対象** | スワイプ(右=翌日 / 左=前日)で1動作にする |
| 延期・削除に到達する導線が無い | **対象** | 行の長押しでメニューを出す(延期・翌日・前日・削除) |
| 誤操作が怖い | **対象** | すべて既存の単段 undo に載せ、トーストの〔↩ 元に戻す〕で取り消せる(§3-3) |
| スワイプが縦スクロールと喧嘩する | **対象** | 方向ロックと `touch-action: pan-y` で分離する(§3-4) |
| スワイプ方向のカスタマイズ設定 | 対象外(将来) | Issue の「**標準では**右=翌日」に対応。設定は作らないが、対応表を1箇所に集めて後から変えやすくする |
| モバイルでの複数選択・一括操作 | 対象外 | 1行1タスクの一覧に選択モードを持ち込むと、打刻優先の設計が崩れる |
| PC(表)へのジェスチャ導入 | 対象外 | PC はキーボードが最速。表の作り込みを崩さない |

## 3. 決定事項(設計方針)

### 3-1. 操作の中身は既存ハンドラをそのまま使う。新規に作るのはジェスチャ層だけ

延期・日付移動・削除の実体は App.tsx に既にあり、**undo・繰り返しタスクの次回日程・
「操作直後は並びを固定してその場に留める」窓(#80)まで実装済み**である。
ジェスチャから同じ関数を呼ぶことで、以下がすべて自動的に揃う。

| 操作 | 呼ぶもの | 既に入っている挙動 |
|---|---|---|
| 延期 | `handlePostpone(task)` | 繰り返しなし→翌営業日 / あり→次回日程。完了・開始済みは対象外 |
| 翌日 / 前日 | `moveFocusedDate("next" / "prev")` | 日付を±1日。undo・並び固定の窓 |
| 削除 | `handleDeleteTask(id)` | undo |

**`moveFocusedDate` にだけ引数を足す。** 現在は「選択 or カーソル位置」しか対象にできないが、
モバイルには選択もカーソルも無い。`handlePostpone(task?)` が既に採っている
「引数のタスク優先 → 選択 → カーソル」の形へ揃える(実装も同じ3行)。

```ts
// 変更前: moveFocusedDate(mode)
// 変更後: moveFocusedDate(mode, task?)   ← handlePostpone と同じ pool の決め方
```

並び固定(`dateShift`)は `visibleTasks` の並び計算そのものに掛かっているため、
モバイル一覧にもそのまま効く。**スワイプした行は undo の窓が閉じるまでその場に留まり、
消えた行を目で探す必要が無い。**

### 3-2. 長押しはボトムシート、スワイプは行そのもの

- **長押し(500ms)** → 画面下から**ボトムシート**を出す。上部にタスク名、項目は
  〔延期〕〔翌日へ〕〔前日へ〕〔削除〕とキャンセル。親指の届く下部に置く。
  画面中央のポップアップにしないのは、長押しした指の位置に依存せず一定の場所に出したいため。
  削除は既存の `handleDeleteTask` を通すので、**従来どおり確認が出てから消える**(§5)。
- **スワイプ** → 行の下から色と文言(→ 翌日 / ← 前日)を覗かせ、指に追従して行を動かす。
  しきい値を越えたら色を濃くして「離せば実行」を示す。

**完了・開始済みのタスクでは〔延期〕を無効表示にし、理由を添える**
(`handlePostpone` はトーストで断るだけなので、押せてしまうと空振りになる)。
翌日・前日・削除は状態によらず有効。

### 3-3. 取り消しは既存のトーストの〔↩ 元に戻す〕をそのまま使う

新しい undo 機構は作らない。既存のトーストは**タップできるボタン**を持っており
(App.tsx の toast。`Ctrl+Z` と同じ `performUndo` を呼ぶ)、キーボードの無いスマホでも成立する。
本変更の操作はすべて `pushUndo` を通る既存ハンドラ経由なので、**追加実装なしで
「元に戻す」が付く**。窓は既存と同じ 6 秒(`UNDO_WINDOW_MS`)。

### 3-4. 縦スクロールとの分離 — ここが失敗しやすい唯一の勘所

#97 で一覧は縦スクロールコンテナになった。横スワイプを足すと衝突するので、次を守る。

- 行に **`touch-action: pan-y`** を指定する。縦方向はブラウザに任せ(慣性スクロールが効く)、
  横方向だけを JS が受け取る。
- **方向ロック**: 最初に 12px 動いた時点で `|dx| > |dy|` なら横、そうでなければ縦と決め、
  以後その回は切り替えない。縦と決まったらジェスチャを放棄する(ブラウザのスクロールに委ねる)。
- ブラウザがスクロールを引き取ると `pointercancel` が飛ぶ。**これはタップではない**ので、
  必ず「何もせず終了」に落とす(ここを取りこぼすと、スクロールしただけで詳細編集が開く)。
- 長押しは 10px でも動いたら取り消す。

判定は DOM に触らない純粋関数に切り出し、単体テストの対象にする。

```ts
lockAxis(dx, dy, slop)          → "horizontal" | "vertical" | null   // null=まだ決まらない
decideSwipe(dx, threshold)      → "next" | "prev" | null
```

### 3-5. スワイプの対応表は1箇所に置く

Issue の「**標準では**右にスワイプ:翌日」に合わせ、対応表を定数1つにまとめる。
設定 UI は作らない(対象外)が、将来入れるときにここだけ差し替えれば済むようにする。

```ts
const SWIPE_ACTION = { right: "next", left: "prev" } as const;  // 右=翌日 / 左=前日
```

**右=翌日(未来)/左=前日(過去)** は、時間軸を左→右に見る感覚と一致する。

### 3-6. 書き込めない端末ではジェスチャを出さない

読み取り専用の窓(#57)と、手番を持たない端末(#91)では、押しても
`ensureWritable()` に弾かれて何も起きない。**ジェスチャ自体を armed にしない**
(長押しメニューを開かず、スワイプも追従させない)。行タップ→詳細編集(閲覧)は従来どおり許可する。

そのために、モバイル一覧へ渡す `readOnly` を**手番なしも含める**よう App 側で直す。

```tsx
// 変更前: readOnly={!isPrimary}
// 変更後: readOnly={!isPrimary || (baton.enabled && baton.role === "guest")}
```

これは #100 の範囲を少しだけ超えるが、`readOnly` が「編集できるか」を正しく表さないまま
ジェスチャを載せると、**押せるのに何も起きない操作**を新設することになるため必要と判断した。
副次的に、手番を持たない端末では打刻ボタンも無効表示になる(#91 §4.3 の意図に沿う)。

### 3-7. ブラウザ標準の長押し動作を抑止する

長押しでテキスト選択や OS のコンテキストメニューが出ると、こちらのメニューと二重になる。
行に `select-none`(`user-select: none`)と `-webkit-touch-callout: none` を当て、
`contextmenu` を `preventDefault` する。**PC の表(TaskTable)には影響させない**
(モバイル一覧のみ)。

## 4. 変更対象

| ファイル | 変更 |
|---|---|
| `src/lib/gesture.ts`(新規) | `lockAxis` / `decideSwipe` と各しきい値。純粋関数のみでテスト対象 |
| `src/lib/gesture.test.ts`(新規) | 方向ロック・しきい値・境界のテスト |
| `src/components/TaskListMobile.tsx` | ポインタイベントの状態機械、スワイプの見た目、長押し検知 |
| `src/components/TaskActionSheet.tsx`(新規) | 長押しで出すボトムシート |
| `src/App.tsx` | `moveFocusedDate` に対象タスク引数を追加(§3-1)、モバイル一覧へ操作ハンドラと正しい `readOnly` を渡す(§3-6) |
| `README.md` | スマホの操作(長押し・スワイプ)を追記 |

### しきい値(初期値)

| 名前 | 値 | 根拠 |
|---|---|---|
| 長押し | 500ms | Android / iOS の標準的な長押しと同じ |
| 長押しの移動許容 | 10px | 指のぶれで取り消されない範囲 |
| 方向ロックの判定距離 | 12px | 10px 前後が一般的。長押し許容より少し大きくして、先に長押しが取り消される順にする |
| スワイプ確定 | 72px | 390px 幅で約 18%。誤爆しにくく、片手で届く |

## 5. 採らない案

| 案 | 理由 |
|---|---|
| 削除の確認を省く(undo があるから) | **既存の `handleDeleteTask` は `confirm()` で確認してから消す。** 実装中の実ブラウザ検証で判明し、本書の初版はここを「確認なし」と誤って記述していた。削除だけは取り消しに気づかず画面を閉じる可能性があるため、既存の確認をそのまま通す(スマホでも同じ挙動になる)。確認と undo の二重の保険は、揺れる電車内での誤操作に対して過剰ではない |
| スワイプで削除 | 一度の指の動きで消えるのは、揺れる電車内では危険度が高い。削除は長押しメニュー(2動作)に置く |
| スワイプ距離ではなく速度(フリック)でも確定 | 実装と調整の手間の割に、距離だけでも十分実用になる。まず距離で運用し、不足を感じたら足す |
| ライブラリ(react-swipeable 等)の導入 | 依存を増やさずに Pointer Events で書ける規模。この repo は依存を最小に保っている |
| 長押しメニューに「開始/終了」も入れる | 打刻は行の常設ボタンで1タップ。メニューへ重複して置くと選択肢が増えるだけ |

## 6. 受け入れ確認(実機)

1. 一覧を上下にスクロールできる(#97 の回帰確認。ジェスチャを足しても慣性スクロールが効く)
2. 行を右へスワイプ → 翌日へ移動。トーストに〔↩ 元に戻す〕が出て、押すと戻る
3. 行を左へスワイプ → 前日へ移動。同上
4. しきい値未満で指を離す → 何も起きず、行が元の位置へ戻る
5. 斜めに動かしても、縦優勢なら**スクロールになり、詳細編集が開かない**
6. 行を長押し → ボトムシートが出る。延期・翌日・前日・削除がすべて効き、いずれも元に戻せる
7. 完了・開始済みのタスクでは〔延期〕が無効表示になり、理由が読める
8. 長押ししてもテキスト選択や OS のメニューが出ない
9. 行を短くタップ → 従来どおり詳細編集が開く
10. スワイプ・長押しの直後、操作した行が**その場に留まる**(undo の窓が閉じてから並び替わる)
11. 読み取り専用の窓・手番を持たない端末では、長押しメニューが出ずスワイプも動かない。行タップの詳細編集は開く
12. PC 幅の表示(表)に変化が無い
