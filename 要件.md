# worklist3 プロジェクト概要書（Claude Code依頼用ドラフト）

## 1. プロジェクト概要

現在Excel（VBAマクロ）で運用しているタスク管理表「worklist」を、ブラウザで動作するアプリとして作り直す。
GTD（Getting Things Done）の考え方をベースに、頭の中にある「やらないといけないこと」「やりたいこと」を全てそこに書き出し、管理する。

## 2. 目的・背景

- 現行のExcelマクロには、タスクの開始・終了・中断（割込み）・繰り返し生成・並び替えなど、実運用で磨かれたロジックが既にある
- これをブラウザアプリとして再構築し、操作性・視認性を向上させる
- 「見積時間」と「実績時間」を記録する仕組みは既にExcel側にあり、これを踏襲する
- Excelには無かった「期限（deadline）」の概念を新たに導入する

## 3. ターゲットユーザー・利用シーン

個人利用。PCブラウザから、業務中に頻繁に開閉して使う。ショートカットキーでの高速なタスク追加を重視。

## 4. 機能要件

### Must（必須）

**タスクの基本**
- タスクの自由な書き出し（GTD的インボックス）
- カテゴリ指定（ビジネス／ファミリー／パーソナル等）で一覧を分けられる
- 重要度：S（最高）〜E の6段階
- 見積時間（分）を設定できる

**日時・期限（別概念として管理）**
- **日時**：いつやるか（開始日時。「何日の何時から」）
- **期限**：いつまでにやるか（締切日）
- 日時が未設定のタスクは、全て「その日のタスク」として扱う
- 表示切替：
  - 「今日のタスクすべて」＝日時設定の有無を問わず、その日に属する全タスク
  - 「今日の予定」＝日時が設定されているタスクのみ
- 期限切れのタスクは目立つ色（赤系）で警告表示する

**繰り返し・ルーティンタスク**
- 毎日／毎週／毎月／毎週◯曜日などの定期タスク
- 完了トリガー型（完了からX日後／X週後に次のタスクを生成）
- ※Excel版は `[r/R][d/w/m/y]数字` という記号入力式だったが、worklist3ではUI（フォーム入力）で分かりやすく指定できるようにする

**ステータス管理**
- ドロップダウン等、分かりやすいUIで選択する固定ステータス
- 候補：未着手／進行中／完了／中断中／待ち（他に必要なものがあれば追加）

**タスク実行中の操作**
- 実行中のタスクに別タスクが割り込んできたら中断できる
  - Excel版ロジック：中断時点で「消化した分」と「残りの見積時間」に分割し、残タスクとして引き継ぐ。割り込みタスク名を入力すれば、その場で新規タスクとしても登録できる
- 中断したタスクは、後から再開できる

**タスクの分解**
- 1つのタスクに対して「準備」（例：会議タスクに対する準備時間）を追加できる
- タスクを複数の子タスクに「分割」できる

**入力・連携**
- ブラウザ上でショートカットキーによる高速なタスク追加
- クリップボードの内容を解析し、Teamsリンク／Outlook予定形式／通常テキストを自動判別してタスク化
  - Excel版の `UnifiedInsertTask` のロジック（URLにteams.microsoft.comを含む→Teams扱い、2行目が日付形式→カレンダー予定扱い、それ以外→通常タスク）を踏襲

**付随情報**
- タスク1件につきリンク最大5件
- メモ欄

### Want（できれば）

- 週単位での予実サマリー（グラフ表示）
- 選択した複数タスクに対して、見積時間を積み上げて連続的に開始時刻を自動設定する機能（Excel版 `SetSequentialStartHHMM` 相当）
- 当日タスクからランダムに1件選んで開始する機能（Excel版 `StartRandomTodayTask` 相当）

### Out of Scope（今回は対象外）

- 複数ユーザーでの共有・アカウント機能
- モバイルアプリ化（今回はブラウザのみ）

## 5. 技術スタック・環境（提案）

- フロントエンド：React + TypeScript
- スタイリング：Tailwind CSS
- データ保存：まずはブラウザ内保存（localStorage / IndexedDB）で個人利用。将来的なサーバー同期は視野に入れつつ、データ層は差し替えやすい設計にする
- 実行環境：Node.js、Vite
- 新規プロジェクト（既存コードなし）

> ※ここは叩き台です。フレームワークやスタイリング手法にご希望があれば変更します。

## 6. 画面・UI要件

- **表形式（Excel踏襲）とカード/リスト形式（Todoist風）を切り替えられるようにする**
- ステータスはドロップダウンで選択（記号入力は廃止）
- 期限切れタスクは赤系で強調表示
- 「今日のタスクすべて」／「今日の予定」の表示切替
- カテゴリでの絞り込み表示

## 7. データ構造・ロジックの要点（Excel版の列構成を踏襲・拡張）

| 項目 | Excel列 | worklist3での扱い |
|---|---|---|
| 日付 | A | 開始日として利用 |
| ステータス | B（記号） | 固定ステータス（ドロップダウン） |
| 繰り返し設定 | C（記号） | フォーム入力に変更 |
| タスク名 | D | そのまま |
| 見積時間（分） | E | そのまま |
| 開始予定時刻 | F | 「日時」の時刻部分 |
| 終了予定時刻 | G（自動計算） | 開始予定＋見積で自動計算、踏襲 |
| 開始実績 | H | そのまま |
| 終了実績 | I | そのまま |
| 実績時間（分） | J（自動計算） | 終了実績−開始実績、踏襲 |
| メモ | K/L/M | 踏襲（3つ） |
| テーマ | N | カテゴリ相当として整理予定 |
| 残り時間 | O | 中断タスクの残見積として踏襲 |
| **期限** | （新規） | worklist3で新規追加する概念 |
| **リンク（最大5件）** | （新規） | worklist3で新規追加 |

**中断（割込み）ロジックの踏襲**
1. 現在のタスクを「終了実績＝今」で確定
2. `見積時間 − 実績時間` を「残り」として新しいタスクに引き継ぐ
3. 割り込みタスク名が入力された場合、その残タスクの前に割込みタスクとして新規登録

**並び替えロジックの参考**（Excel版）
日付 → 開始実績 → 開始予定 → ステータス → 繰り返し → 見積時間 → タスク名、の優先順位。worklist3でも同様のソート・フィルタができると使い勝手が近くなる。

## 8. 非機能要件

- オフラインでも動作すること
- ページリロードしてもデータが消えないこと
- 個人利用のため、過度なパフォーマンスチューニングは不要

## 9. 進め方・確認方法の指定

- まず全体のディレクトリ構成と、実装するファイル一覧を提示してから着手してほしい
- 機能単位（例：タスクCRUD → 日時/期限管理 → ステータス/繰り返し → 中断・分割・準備 → 表示切替・並び替え）で区切り、都度動作確認できる状態にしてほしい
- ステータスの選択肢や繰り返し設定のUI仕様など、実装中に判断が必要な箇所は、着手前に一度提案してほしい

## 10. 成果物・完了条件

- ローカルで `npm run dev` 等で起動できる状態
- Must要件が一通り動作すること
- READMEに起動手順を記載すること

## 11. 補足・参考情報

- 元となるExcelマクロ（modAlarm / modCommon / modConfig / modFilter / modSchedule / modTable / modTask / modTaskCreate）のロジックを参考にしている
- 特に「中断（割込み）」「繰り返しタスク生成」「並び替え・条件付き書式」のロジックは、Excel版の実装意図をなるべく引き継ぎたい
- Excel版にあった「アラーム機能（modAlarm）」は今回のMust/Wantには含めていない。必要であれば追加を検討

---

### 未確定・要検討事項
- ステータスの選択肢は「未着手／進行中／完了／中断中／待ち」の5つで過不足ないか
- 期限が「当日」「◯日前」に近づいた場合の警告表示要否（現状は期限超過のみ強調の想定）
- 技術スタックの最終確認（React+TypeScript+Tailwindで問題ないか）
