# worklist3 変更仕様書：引き継ぎを3つに分ける

> 本書は Claude Code に実装を依頼するための変更仕様書です。Issue #109 に対応します。
> `変更仕様書_複数台利用.md`（#91、手番制＝バトンパス）の §4.5「奪取」を作り直します。
> **完全自動の同期は目的ではありません。** どのデータで続けるかを人が選べるようにするだけです。

## 1. 目的

いま、手番を持たない端末にある選択肢は **「読み込んで、この端末で更新する」の一択**です。
この操作は3つのことを同時に行います。

1. Drive のミラーを読む
2. 手元のタスクを**丸ごと置き換える**
3. 手番を自分にする

Issue #109 は、この束ね方が現実と噛み合っていない、という報告です。

**端末Aで編集 → バックアップが終わる前にAを離れる → 端末Bで引き継ぐ → Bはミラー（＝Aの最後に成功した時点）を読む。**
このとき **Aの未送信の編集は、Aの localStorage にしか存在しません。** そしてAに戻って
バナーの唯一のボタンを押すと、②によって**その未送信分が消えます**（`App.tsx:865-870`。
undo も積まれません）。アプリが促すボタンが、生きているデータを消す経路になっています。

やりたいことは、実は状況によって違います。

| 状況 | やりたいこと | いまできるか |
|---|---|---|
| 出先で予定を**見たいだけ** | 読む。手番はいらない | **できない**（読むと手番まで取ってしまう） |
| **手元が最新**（相手はまだ何も更新していない） | 手番だけ取る。手元は捨てない | **できない**（読み込みが強制される） |
| 相手の成果を引き継いで続ける | 読んで、手番も取る | できる（現状の唯一の道） |

## 2. 潰す対象と、あえて対象外にするもの

| 課題 | 扱い | 方針 |
|---|---|---|
| 見たいだけなのに手番を取ってしまう | **対象** | 「見るだけ」を独立した操作にする（§4.2） |
| 手元が最新なのに読み込みで消される（#109 の本体） | **対象** | 「読み込まずに引き継ぐ」を足す（§4.3） |
| 読み込まずに引き継ぐと、相手の成果が消えうる | **対象** | 引き継ぐ直前に**現ミラーを退避**する（§4.3） |
| 退避・救出したファイルを読み戻せない | **対象** | 復元の一覧に出す。ただし既定は追加読込（§4.4） |
| 手元と相手の**自動マージ** | **対象外** | #91 が却下した道。人が選ぶ方式を維持する |
| 未送信を無くすこと（デバウンスの短縮など） | **対象外** | 別テーマ。本書は「取りこぼした後に救えること」に絞る |
| 3台以上での運用 | 対象外 | 仕組みとしては動くが、想定は2台 |

## 3. 決定事項（設計方針）

### 3-1. 2つの軸に分ける

いまの「奪取」は2つの独立した判断を束ねています。**束ねているから事故が起きます。**

- **画面のデータをどうするか** … Drive の内容にするか、手元のままにするか
- **手番をどうするか** … 取るか、取らないか

この2軸で並べると、意味のある組み合わせは3つです。

| 操作 | 画面のデータ | この端末の localStorage | 手番 |
|---|---|---|---|
| **A. 見るだけ** | Drive の内容 | **触らない** | 取らない |
| **B. 読み込んで引き継ぐ**（現状維持） | Drive の内容 | Drive の内容で置換 | 取る |
| **C. 読み込まずに引き継ぐ** | 手元のまま | **触らない** | 取る（＋現ミラーを退避） |

残る「手元のままで手番を取らない」は、いまの読み取り専用そのものなので操作として不要です。

### 3-2. 「見るだけ」は localStorage を一切触らない

見るだけの操作で手元が書き換わるなら、それは #109 と同じ事故です。
**画面（メモリ）だけを差し替え、`localStorage` には書きません。** リロードすれば自分のデータに戻ります。

このため閲覧中は、**保存もバックアップも完全に止めます**。手番を持つ端末が閲覧に入った場合も
同じです（止めないと、Drive の内容をそのまま Drive へ書き戻し、その端末の未送信分が永久に届かなくなる）。

### 3-3. 閲覧中であることは、常に画面に出す

見ているものと保存されているものが食い違う状態を作る以上、**それを隠してはいけません。**
専用のバナーを常時出し、そこから2つの出口（自分のデータに戻す／編集を有効にする）へ
すぐ行けるようにします。

### 3-4. 「読み込まずに引き継ぐ」は、相手の成果を消しうる。だから先に退避する

Cを選ぶと、その後の最初のバックアップで**手元の内容がミラーを上書きします**。
相手がすでにバックアップ済みの編集を持っていた場合、それは失われます。

日次コピーは同じ日なら同名で上書きされるため、**その日のうちの事故は日次コピーでは救えません。**
よって **Cの実行直前に、現ミラーを別名で Drive へ退避します**（§4.3）。

退避は「相手の成果を消す事故」に対する唯一の網ですが、**書けなくても操作は止めません。**
止めると、圏外や権限切れのときに永久に引き継げなくなり、#84 の閉じ込めを繰り返します。

### 3-5. 系譜が1本という不変条件は、Cで意図的に破る

#91 の不変条件は「データの系譜は常に1本」でした。**Cはこれを人の明示操作で破ります。**
破ったことは退避ファイルという形で必ず記録に残し、後から突き合わせられるようにします。
これは「壊れないこと」ではなく「壊れたときに気づけて、拾えること」を選ぶ判断です。

## 4. 機能要件（詳細仕様）

### 4.1 3つの操作の入口

**手番を持たない端末のバナー**（既存の `BatonBanner`）に、3つを並べます。

```
　この端末は読み取り専用です
　更新中の端末: 自宅PC
　最終バックアップ: 2026/09/06 09:12（3分前）／431件
　いまの手元: 428件

　〔読み込んで、この端末で更新する〕　〔見るだけ〕　〔手元のまま更新する〕
```

- **既定（強調）は従来どおり B**。いちばん安全で、いちばん多い操作だから
- C（手元のまま更新する）は確認ダイアログを必ず挟む（§4.3）
- 幅が足りない端末では、B を主ボタン、A と C をその下の小さな並びにする

**独立した入口**として、💾メニューにも「見るだけ」を置きます（§4.2）。
手番の有無に関わらず使えます。

### 4.2 見るだけ（閲覧モード）

**入口は2つ。** バナーの〔見るだけ〕（ミラー直行）と、💾メニューの
「👁 Drive の内容を見る（手元は変えません）」。後者は既存の復元一覧を再利用し、
ミラー・日次コピー・退避ファイルから選べます。

**ふるまい**

- 画面のタスクを、選んだ控えの内容に差し替える
- **`localStorage` には書かない。** `repository.save()` を呼ばない
- **バックアップも書かない。** `notifyTasksChanged()` を呼ばない
- 編集操作はすべて弾く（既存の `ensureWritable()` に閲覧中の判定を足す）
- リロード・再起動で自分のデータに戻る（閲覧中であることは記憶しない）

**常時バナー（§3-3）**

```
　👁 Drive の内容を見ています（2026/09/06 09:12 の控え ／ 431件）
　この端末に保存されているデータは変えていません（428件）

　〔自分のデータに戻す〕　〔編集を有効にする〕
```

- 〔自分のデータに戻す〕… `repository.load()` で画面を戻し、閲覧モードを抜ける
- 〔編集を有効にする〕… **どちらのデータで続けるかを必ず選ばせる**（§4.2.1）

### 4.2.1 閲覧中に「編集を有効にする」を押したとき

ここを曖昧にすると #109 が形を変えて再発します。**必ず選択肢を出します。**

```
　どちらのデータで編集を始めますか？

　〔いま見ている Drive の内容で続ける〕
　　　この端末のデータ(428件)は Drive の内容(431件)で置き換わります
　　　＝「読み込んで引き継ぐ」と同じ

　〔この端末のデータに戻して続ける〕
　　　いま見ていた内容は破棄し、この端末の428件で続けます
　　　Drive 側は、この端末の内容で上書きされます
　　　＝「読み込まずに引き継ぐ」と同じ（現ミラーは退避します）

　〔やめる〕
```

つまり閲覧モードは、**BとCへの共通の踊り場**として機能します。

### 4.3 読み込まずに引き継ぐ

**手順**（この順序を変えないこと）

1. **現ミラーを取得する**（`readMirror()`。件数と最終更新時刻を確認材料にする）
2. **確認ダイアログを出す**（下記）
3. **現ミラーを退避ファイルとして書き出す**
4. 手番ファイルを自分に書き換える
5. **サニティガードの比較元を手元の件数に合わせ、最初のバックアップを明示実行する**

**確認ダイアログ**

```
　この端末のデータで、更新を引き継ぎます。

　この端末: 428件
　Drive の最新: 431件（2026/09/06 09:12 ／ 自宅PC）

　⚠ Drive 側の431件は、この端末の428件で上書きされます。
　　自宅PC でしか行っていない変更があれば、それは失われます。

　上書きされる前の Drive の内容は、退避ファイルとして残します。
　　worklist3-<グループ名>-引継前-9f2c8a-20260906-0915.json.gz

　〔この端末のデータで引き継ぐ〕　〔やめる〕
```

- **件数と時刻は必ず両方出す**（どちらが新しいかを人が判断できるように）
- **相手の件数の方が多い場合は、その差を強調する**
- この操作に undo は無い。ダイアログにそう書く

**手順3が失敗したとき（オフライン・権限切れ・書き込み失敗）**

```
　⚠ Drive の現在の内容を退避できませんでした（オフラインの可能性があります）。

　このまま引き継ぐと、Drive 側の431件は、この端末の内容で上書きされ、
　元に戻す手段がありません。

　〔それでも引き継ぐ〕　〔やめる〕
```

**止めない。** 警告して人に決めさせます（§3-4）。

**手順5について**

`lastBackedUpCount` は保存先ごとの比較元です。Cの直後は「431件 → 428件」に見えるため、
条件によってはサニティガードが働いて**バックアップが止まります**。引き継いだのに控えが
取れない状態は危険なので、**Cの完了時に比較元を手元の件数へ更新し、最初の1回は
明示操作扱い（force）で書きます。**

### 4.4 退避ファイルと救出ファイルを読み戻せるようにする

現在、救出ファイル（#91 §4.6）は復元の一覧に**出てきません**。
`listDaily()` が日次の命名規約に一致する名前しか拾わないためです（意図的にそうしてある）。
その結果、書き出しても実質的に読み戻せません。本変更で退避ファイルが増えるので、ここを塞ぎます。

- 復元ダイアログに **「退避・救出（自動では消えません）」** の節を設け、一覧に出す
- **この節から選んだときの既定は「追加で読み込む（merge）」**とする。
  全リセット読込を選ぶには、確認をもう一段挟む
- 一覧には**種別・日時・端末ID の先頭・件数**を出す。「最新の控え」と取り違えさせないため

**なぜ既定を merge にするか**：退避・救出は「本流から外れた断片」です。
それで本流を丸ごと置き換えるのは、直すつもりで新しい喪失を作る操作です。

### 4.5 命名

退避ファイルは、日次コピーのローテーション掃除に巻き込まれないよう、
規約に**一致させない**名前にします（救出ファイルと同じ考え方）。

```
worklist3-<グループ名>-引継前-<端末IDの先頭6桁>-YYYYMMDD-HHmm.json(.gz)
```

`rescueFileName()` を、種別を引数に取る形へ一般化して共用します。

### 4.6 手番制OFF・窓ロックとの関係

- **手番制OFF**：BとCは意味を持たないので出さない。**A（見るだけ）は使える**
- **窓ロック（#57）で secondary の窓**：閲覧は許可する（書かないので安全）。
  編集を有効にする操作は、従来どおり窓の引き継ぎを先に要求する
- 閲覧中の表示は、手番なしのバナーより**優先して出す**（いま画面に出ているものが
  何なのかが最も重要な情報のため）

## 5. 実装方針（技術メモ）

- **状態遷移は純粋関数へ切り出して単体テストの対象にする**（このリポジトリの方針）。
  - `takeoverPlanOf(mode, mirrorCount, localCount, mirrorModifiedTime, now)` … 確認ダイアログに出す文言の材料と、警告の有無を決める
  - `canSave(viewing, batonRole, isPrimary)` … 保存してよいかの唯一の判定。
    **閲覧中は必ず false**。`App.tsx` の保存 `useEffect` と `ensureWritable()` の両方がここを通る
- 閲覧モードの状態は **`App.tsx` の state（メモリのみ）**。localStorage にも Drive にも書かない
- 退避の書き出しは `gdrive.ts` に置く（`writeRescue()` の隣）。FSA では手番制を提供しないので
  インターフェースは汚さない
- 復元一覧は `listDaily()` に加えて、退避・救出を拾う `listRescues()` を足す
- 既存のインポート経路（`runImport`）はそのまま使う。**新しい書き換え経路は作らない**

## 6. 危険と、その塞ぎ方（実装時に必ず確認すること）

| # | 危険 | 塞ぎ方 |
|---|---|---|
| 1 | 閲覧中に保存が走り、手元が Drive の内容で潰れる | 保存経路を `canSave()` 1か所に集約し、閲覧中は必ず false。単体テストで固定 |
| 2 | 閲覧中にバックアップが走り、手番を持つ端末の未送信分が永久に届かなくなる | 閲覧中は `notifyTasksChanged()` を呼ばない |
| 3 | Cで相手のバックアップ済みの編集が消える | 実行前に現ミラーを退避。退避できなければ警告して人に決めさせる |
| 4 | Cの直後にサニティガードが働き、バックアップが止まる | 比較元を手元件数へ更新し、最初の1回は force |
| 5 | 退避・救出を「最新の控え」と取り違えて全リセット読込する | 一覧を別の節に分け、既定を merge、種別と日時と件数を明示 |
| 6 | 閲覧のまま編集したつもりになる | 常時バナー＋編集操作を全部弾く |
| 7 | 退避ファイルが溜まる | 掃除しない（規約外の名前）。README に「溜まったら手で消す」と明記 |

## 7. 完了条件（受け入れ基準）

1. 手番の無い端末で〔見るだけ〕を押すと、画面が Drive の内容になり、**リロードすると自分のデータに戻る**
2. 閲覧中は編集できず、常時バナーが出ている
3. 閲覧中に〔編集を有効にする〕を押すと、**どちらのデータで続けるかを必ず聞かれる**
4. 〔手元のまま更新する〕を選ぶと、確認ダイアログに**両方の件数と時刻**が出る
5. 同操作の後、Drive に `-引継前-` の退避ファイルができている
6. 同操作の後、**バックアップが正常に走る**（サニティガードで止まらない）
7. 退避・救出ファイルが復元の一覧に出て、**既定が追加読込**になっている
8. 手番制OFFでも〔見るだけ〕が使える
9. 上記の判定ロジックが純粋関数として単体テストされている
10. 既存のテストとビルドが通る

## 8. 採らなかった案

- **手元と Drive の自動マージ**：#91 が却下したとおり。系譜を1本に保つ設計と噛み合わず、
  tombstone まで必要になる。人が選ぶ方式で足りる
- **Cのときに相手の差分を画面で突き合わせる**：あれば親切だが、実装量が跳ね上がる。
  件数と時刻を見せて人が判断する、で始める
- **未送信を無くす（デバウンス短縮・常時同期）**：本書の範囲外。取りこぼしを完全に消すには
  常時同期が要り、それは #91 の目的ではない
- **「見るだけ」で localStorage も更新する**：見ているものと保存が常に一致して分かりやすいが、
  未送信を抱えた端末が押した瞬間に #109 と同じ事故が起きる。採らない
