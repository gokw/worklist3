# worklist3 変更仕様書：Google カレンダー連携（Claude Code 依頼用）

## 1. 概要・目的

worklist3 で管理している「予定」（開始時刻が入ったタスク）を、ボタン操作で Google カレンダーに登録できるようにする。
これまでは予定をコピーして生成 AI に解釈させ、手作業でカレンダーに入れていた。worklist3 には既に日時・見積が構造化データとして存在するため、その解釈工程を不要にし、ワンクリックで正確に同期する。

## 2. スコープ

### 対象（Must）

- 「予定」タスク（`date` と `planStart` の両方が入ったタスク）を、選択して Google カレンダーへ登録・更新する
- 一度登録した予定を、再度ボタンを押すことで最新内容に更新（上書き）できる
- 個人利用・特定 1 台のブラウザからの利用

### 対象外（Out of Scope）

- カレンダー → worklist3 への逆同期（カレンダー側の変更・削除は worklist3 に一切反映しない）
- worklist3 側の削除・予定解除に伴うカレンダーイベントの削除（後述の「放置」方針）
- 繰り返し設定（`repeat`）をカレンダーの繰り返しイベント（RRULE）へ変換すること
- 自動同期（タスク変更時に自動でカレンダーへ送る機能）
- リフレッシュトークンによる無人同期／バックエンドサーバー
- 複数アカウント・複数カレンダーの同時運用

## 3. 全体方式

- **ブラウザ完結型**：バックエンドを追加せず、SPA から Google カレンダー API を直接呼ぶ。認証は Google Identity Services（GIS）のトークンフロー。
- **一方向同期**：worklist3 → カレンダーの **create / update のみ**。**delete は一切行わない**。
- **専用カレンダー**：普段使いの `primary` には書かず、worklist 専用カレンダーを 1 本手動で作成し、そこへ書き込む。
- **手動 upsert**：ユーザーが対象を選び、ボタンを押したときだけ同期する。押すたびに「現在値で上書き」するため、変更検知ロジックは持たない。

### 方式選定の背景（実装時の判断材料）

- worklist3 はインライン編集・実績時刻の記録・中断分割などでタスクが頻繁に更新される。その大半（実績・ステータス・メモ）はカレンダーに無関係なため、自動同期は発火条件が複雑になりエッジケースが増える。手動にして「押したら最新で上書き」に倒すことで、実装量とエッジケースを最小化する。
- 専用カレンダーは、放置された不要イベントが溜まってもカレンダーごと消して再同期できる、表示 ON/OFF がワンタッチ、という利点があり「delete しない（放置）」方針と噛み合う。

## 4. データモデル変更（`src/types.ts`）

`Task` に **1 フィールドだけ**追加する。

| フィールド | 型 | 説明 |
|---|---|---|
| `gcalEventId` | `string \| undefined` | 同期済みの場合に保持する Google カレンダーのイベント ID。未同期なら `undefined`。 |

- スナップショットやハッシュ、同期日時などの**追加保持はしない**（変更検知を行わないため不要）。
- 既存データとの後方互換：`gcalEventId` は任意フィールドなので、`src/lib/storage.ts` の `migrateTask` で「無ければ `undefined`」として扱えばよい。マイグレーションでの値の補完は不要。
- JSON エクスポート／インポート（Issue #12 経路）でも、他フィールドと同様に持ち回るだけでよい。

## 5. マッピング仕様（Task → カレンダーイベント）

同期対象は「`date` と `planStart` の両方がある」タスクのみ。イベントは **件名・開始・終了だけ**のミニマル構成とする。

| イベント項目 | 生成元 | 規則 |
|---|---|---|
| `summary` | `title` | そのまま。カテゴリ等の接頭辞は付けない。 |
| `start.dateTime` | `date` + `planStart` | 例：`date="2026-07-16"`, `planStart="14:30"` → `2026-07-16T14:30:00` |
| `start.timeZone` | 固定 | `Asia/Tokyo` |
| `end.dateTime` | 開始 + `estimateMin` | 見積分を開始に加算。日付跨ぎは `dateTime` がそのまま表現する。 |
| `end.timeZone` | 固定 | `Asia/Tokyo` |

補足規則：

- **見積が未設定または 0 のとき**：所要時間を **15 分**として `end` を算出する。
- **`description` は設定しない**（メモ・リンクはカレンダーに出さない。ユーザーの明示要望）。
- **色分け（`colorId`）は設定しない**（専用カレンダー 1 本で区別できるため）。
- カレンダーに載せないフィールド：`scope` / `importance` / `waiting` / `actStart` / `actEnd` / `deadline` / `repeat` / `category` / ステータス。
  - `deadline`（締切）は「作業する時刻」とは別概念のため、予定イベントには含めない。

## 6. 対象タスクの選択と UI

既存の複数選択機構（`selectedIds`、Space トグル、Shift+↑↓／Shift+クリックの範囲選択、全選択／選択解除）にそのまま乗る。

### 6.1 ボタン追加

- `src/components/Toolbar.tsx` の 1 段目右側、既存の「⏱ 連続時刻 (n)」「✏️ 一括編集 (n)」の並びに **「📅 カレンダー登録 (n)」** を追加する。
- 挙動は既存 2 ボタンに合わせる：`selectedCount === 0` のとき無効、選択件数 `(n)` を表示。
- クリックで `selectedIds` を対象に同期処理（第 8 章）を実行する。
- App 側に `onSyncCalendar` 相当のハンドラを追加し、`Toolbar` の Props に渡す（既存の `onSequentialStart` / `onBulkEdit` と同じ流儀）。

### 6.2 想定操作フロー（すべて既存機能で完結）

1. ツールバーの「予定のみ」トグルを ON（`planStart` があるものだけ表示）
2. 「全選択」または個別選択で対象を選ぶ
3. 「📅 カレンダー登録 (n)」を押す
4. 結果ダイアログ（第 6.3）で件数を確認

### 6.3 適格外タスクの扱いと結果表示

- 選択の中に「予定でない」タスク（`planStart` なし）が混ざっていても**エラーにせず、黙ってスキップ**する。
- 送信結果は `src/components/ImportResultDialog.tsx` と同じ作りの結果ダイアログで表示する。表示項目：
  - 新規作成：○件
  - 更新：○件
  - スキップ（時刻なし）：○件
  - 失敗：○件（失敗理由を簡潔に）
- 少数・単純なケースは既存の `showToast` でも可（実装判断）。

### 6.4 行ごとの同期アクションは設けない（v1）

- 行の操作メニュー（開始／終了／中断／コピー等）に個別送信は**追加しない**。1 件だけ送りたい場合はその 1 件を選択して同ボタンを使う（入口を 1 つに保つ）。

### 6.5 同期済みマークは表示しない（v1）

- `gcalEventId` の有無を一覧に表示するインジケータは v1 では**設けない**。必要になったら後から行に小さなアイコンを足せる設計とする。

## 7. 認証フロー（Google Identity Services トークンフロー）

- 認証は**「📅 カレンダー登録」ボタン押下（ユーザー操作）を起点**に実行する（ポップアップブロック回避のため）。
- **初回のみ**同意画面が出る。以降は `prompt: ''` でサイレントにアクセストークンを再取得する（ブラウザに Google ログインセッションがあれば画面は出ない）。
- 取得したアクセストークンは **メモリ保持のみ**。localStorage 等へ永続化しない。リロード後の初回押下時に取り直す。
- スコープは **`https://www.googleapis.com/auth/calendar.events` のみ**。
- **リフレッシュトークンは使わない**（ブラウザフローでは発行されない。無人同期は非対象）。

## 8. 同期処理ロジック（upsert）

選択タスクを 1 件ずつ処理する。**変更検知はせず、常に現在値で書き込む。**

1. `date` または `planStart` が無い → スキップ（第 6.3 で報告）。
2. `gcalEventId` が **無い** → `events.insert`（新規作成）。成功したら返却された `id` を `gcalEventId` としてタスクに保存する。
3. `gcalEventId` が **有る** → `events.patch`（現在値で上書き更新）。
   - patch が **404（カレンダー側で手動削除されていた等）** の場合 → **`events.insert` にフォールバック**して作り直し、新しい `id` で `gcalEventId` を更新する。
4. 書き込み先の `calendarId` は、設定に保存された worklist 専用カレンダーの ID（第 9 章）。

### バッチ時の振る舞い

- 複数件を処理する途中で 1 件失敗しても**止めずに続行**し、最後に結果をまとめて報告する。
- 失敗した件は `gcalEventId` を書かない（＝未同期のまま）ので、**もう一度ボタンを押せばリトライ**になる。
- **トークン切れ（401）**：裏でトークンを取り直し、その件を **1 回だけ自動リトライ**する。
- **レート超過・権限不足（403）／ネットワーク断**：失敗として計上し、結果ダイアログで報告（押し直しでリトライ）。

## 9. 設定値の保持（Client ID / Calendar ID）

`.env` は使わず、**アプリ内の設定欄に入力 → localStorage に保存**する（このアプリの「設定はブラウザに持つ・UI で設定する」流儀に合わせる）。

- 保存先：`localStorage`（プレーン文字列。IndexedDB や権限は不要。バックアップ機能のフォルダハンドルのような「再接続」は発生しない）。
  - キー例：`worklist3.gcal.clientId` / `worklist3.gcal.calendarId`
- 設定 UI：`src/components/Toolbar.tsx` の 💾 データメニュー内に「Google カレンダー連携」セクションを設け、以下を置く。
  - **Client ID** 入力欄
  - **Calendar ID** 入力欄（手動作成した専用カレンダーの ID）
  - **連携をリセット（サインアウト）** ボタン：メモリ上のアクセストークンを破棄し、必要なら再同意からやり直せるようにする。設定値（Client ID / Calendar ID）を消すかは実装判断だが、トークン破棄は必須。
- Client ID・Calendar ID はいずれも機微情報ではない（Client ID はブラウザに露出して安全な値）。長期保存してはならないのは**アクセストークン**だけであり、それはメモリ保持（第 7 章）で担保する。

## 10. セットアップ手順（前提作業・1 回のみ）

### 10.1 Google Cloud 側

1. Google Cloud でプロジェクトを作成する。
2. **Google Calendar API** を有効化する。
3. OAuth 同意画面を設定：**User type = External / 公開ステータス = Testing** のまま、**自分自身をテストユーザーに追加**する（この状態なら Google の審査は不要）。
4. スコープは **`calendar.events`** のみ。
5. **OAuth クライアント ID** を発行：種別 **ウェブアプリケーション**。
   - **承認済みの JavaScript 生成元**：`https://10-212-41-20.sslip.io`
   - （生の IP アドレスは生成元に登録できないため、sslip.io のワイルドカード DNS 経由のホスト名を使う。`10-212-41-20.sslip.io` は自動的に `10.212.41.20` へ解決される。）
6. 発行された Client ID を、アプリの設定欄（第 9 章）に入力する。

### 10.2 カレンダーの用意

1. Google カレンダーで **worklist 専用カレンダーを 1 本手動作成**する。
2. そのカレンダー ID を、アプリの設定欄（第 9 章）に入力する。
3. 手動作成のため、スコープは `calendar.events`（イベント操作）の最小のままでよい（カレンダー自体を API で作らない）。

### 10.3 稼働 PC 側（アクセス URL と証明書）

- アプリを稼働している別デスクトップ PC で、アクセス URL を **`https://10-212-41-20.sslip.io`** とする（従来の `https://10.212.41.20` から変更）。
- HTTPS 証明書を、このホスト名（`10-212-41-20.sslip.io`）向けに用意し直す（SAN に含める）。利用するクライアント PC でこの証明書を信頼させる。
- GIS は非 localhost の生成元に HTTPS を要求するため、HTTPS でのアクセスは必須。

## 11. エラー処理一覧（まとめ）

| 事象 | 処理 |
|---|---|
| 選択に予定でないタスクが混在 | スキップし、結果ダイアログで「時刻なし ○件」と報告 |
| 同意キャンセル／取得失敗 | トーストで通知し、同期を中止 |
| アクセストークン切れ（401） | 裏で取り直し、その件を 1 回だけ自動リトライ |
| patch が 404（イベントが存在しない） | `events.insert` にフォールバックして作り直し、`gcalEventId` を更新 |
| レート超過・権限不足（403） | 失敗として計上し報告 |
| ネットワーク断・その他失敗 | 失敗として計上し報告（未同期のまま＝押し直しでリトライ可能） |
| Client ID / Calendar ID 未設定 | 同期を実行せず、設定を促すメッセージを表示 |

## 12. 非機能・制約

- オフライン時・未設定時でも、既存のタスク管理機能は従来どおり動作すること（連携は付加機能で、失敗しても本体を止めない）。
- アクセストークンをストレージに永続化しないこと。
- 追加スコープは `calendar.events` のみに限ること。

## 13. 完了条件・受け入れ基準

- `date` と `planStart` を持つタスクを選択し、「📅 カレンダー登録」で専用カレンダーに新規イベントが作られること。
- 同じタスクを内容変更後に再登録すると、**重複作成されず**、既存イベントが更新されること（`gcalEventId` による upsert）。
- 見積のないタスクが 15 分のイベントとして登録されること。
- 予定でないタスクを含めて全選択しても、予定でないものはスキップされ、結果が件数で報告されること。
- カレンダー側でイベントを手動削除した後に再登録すると、404 フォールバックで作り直されること。
- worklist3 側でタスクを削除・予定解除しても、カレンダー側には何も起きないこと（放置）。
- 初回のみ同意画面が出て、2 回目以降は画面なしで登録できること。

## 14. 実装の進め方（機能単位・要件.md の方針に合わせる）

1. まず追加・変更するファイル一覧と、設定 UI・ボタンの配置案を提示してから着手する。
2. 機能単位で区切り、都度動作確認できる状態にする。目安の区切り：
   1. データモデル（`gcalEventId` 追加）＋設定保持（localStorage・設定 UI）
   2. 認証（GIS トークン取得・メモリ保持・リセット）
   3. マッピング＋同期処理（insert / patch / 404 フォールバック）
   4. ツールバーのボタン＋結果ダイアログ＋バッチ・エラー処理
3. Google Cloud のクライアント ID 発行・専用カレンダー作成・証明書の差し替えは利用者側の手作業（第 10 章）。実装は「設定値を入力すれば動く」状態を目標にする。

## 15. 検証方針

認証（OAuth 同意・トークン取得）と実際のカレンダー API 呼び出しは、外部の Google アカウント・登録済み生成元・ブラウザのポップアップ・ネットワークを要するため、**Claude Code の内部（自動実行環境）では検証できない**。この制約を前提に、「検証できない層を薄く切り出し、それ以外を自動検証する」設計とする。

### 15.1 設計上の要請（テスト可能性のため）

- **認証＋HTTP 送信を、差し替え可能なアダプタ（インターフェース）として分離**する。例：`CalendarClient`（`insertEvent` / `patchEvent` を持つ）と、その本番実装（GIS トークン＋fetch）。
- マッピング・upsert 判定・バッチ／エラー処理は、この `CalendarClient` を受け取る**純粋なロジック**として実装し、テストではモック実装を差し込む。
- これにより、ネットワークも Google アカウントも無い環境で、以下の大半を自動検証できる。

### 15.2 Claude Code が内部で自動検証する範囲（Must）

- 型チェックとビルドが通ること。
- **マッピング関数**（Task → イベント本体）の単体テスト：
  - `date` + `planStart` → `start.dateTime`（例：`2026-07-16` + `14:30` → `2026-07-16T14:30:00`）
  - `estimateMin` 加算による `end.dateTime`
  - 見積が未設定／0 のとき **15 分**になること
  - `timeZone` が `Asia/Tokyo` であること
  - `description`・`colorId` を含まないこと
- **upsert 判定**：`gcalEventId` 無 → insert、有 → patch を選ぶ分岐。
- **バッチ挙動**（モックの `CalendarClient` を使用）：
  - 予定でないタスク（`planStart` なし）をスキップし、結果件数に計上する
  - 1 件が失敗しても処理を止めず、最後に結果を集計する
  - 成功した件のみ `gcalEventId` が更新され、失敗した件は未同期のまま残る
- **404 → insert フォールバック**：patch が 404 を返すモックで、insert が呼ばれ `gcalEventId` が更新されること。
- **401 → 1 回だけ自動リトライ**：最初の呼び出しが 401、再取得後に成功するモックで、正しくリトライされること。
- `migrateTask` が `gcalEventId` を破壊しない／未設定を許容すること。

### 15.3 利用者が実環境で手動確認する範囲（第 13 章の受け入れ基準）

実環境（`https://10-212-41-20.sslip.io`・実 Google アカウント・専用カレンダー）で、利用者が確認する。

- 初回のみ同意画面が出て、2 回目以降は画面なしで登録できること。
- 専用カレンダーに実イベントが作られること／再登録で重複せず更新されること。
- カレンダー側で手動削除した後の再登録が、404 フォールバックで作り直されること。
- worklist3 側の削除・予定解除でカレンダーに何も起きないこと（放置）。

### 15.4 手動確認を軽くする補助機能

- **ドライラン（プレビュー）**：API を呼ばず、「何を送るか（件名・開始・終了）」を一覧表示するだけのモードを設ける。マッピングの正しさを round trip なしで目視でき、テスト補助にもなる。実装するかは任意だが推奨。
- **localhost を第 2 の生成元に登録（任意）**：ブラウザ側 PC でも開発サーバーを立てれば、承認済み JavaScript 生成元に `http://localhost:<port>` を追加することで、OAuth 実フローを手元でも試せる。実地確認の場所を増やせる。

## 16. 未確定・要検討事項

- 「連携をリセット」で Client ID / Calendar ID まで消すか、トークンだけ破棄するか（現状はトークン破棄を必須、設定値の扱いは実装判断）。
- 結果表示を専用ダイアログにするか、軽微なケースはトーストで済ませるかの線引き。
- 自己署名証明書をクライアント PC で信頼させる具体手順（運用メモとして別途）。
